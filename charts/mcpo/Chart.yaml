apiVersion: v2

name: mcpo
description: A description
version: 0.0.1
appVersion: v0.0.1
type: application

keywords:
  - mcpo
  - chat
  - web-ui
  - open-webui

dependencies:
  - name: bjw-template
    # Bumped from 4.1.2 → 4.6.2 as part of the local-fork normalization.
    # Render-verified. See bjw-s changelog for 4.2.x → 4.6.x notes.
    version: "4.6.2"
    alias: mcpo
    repository: file://../bjw-template