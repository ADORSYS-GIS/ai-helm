apiVersion: v1
kind: Service
metadata:
  name: librechat
  namespace: {{ .Release.Namespace | default .Values.namespace }}
  labels:
    app: librechat
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3080
    targetPort: 3080
    protocol: TCP
  selector:
    app: librechat
