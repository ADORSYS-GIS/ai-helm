apiVersion: v1
kind: Service
metadata:
  name: phoenix
  namespace: {{ .Release.Namespace | default .Values.namespace }}
  labels:
    app: phoenix
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 6006
    targetPort: 6006
    protocol: TCP
  selector:
    app: phoenix
