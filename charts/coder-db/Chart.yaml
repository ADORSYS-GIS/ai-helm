apiVersion: v2
name: coder-db
description: |
  CloudNative PG Cluster (coder-db) for the Coder platform, plus its
  barman-cloud ObjectStore, ScheduledBackup, PodMonitor, a managed `coder`
  role, and the `coder` Database CR. Leaf chart of the coder App-of-Apps
  orchestrator (ADR-0082).
type: application
version: 0.1.0
appVersion: "2.33.7"