---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: terraform
  namespace: {{ include "common.names.namespace" $ }}
spec:
  endpoints:
    - fqdn:
        hostname: terraform-mcp.{{ include "common.names.namespace" $ }}.svc.cluster.local
        port: 8080