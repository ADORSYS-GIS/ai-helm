---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: brave-search
  namespace: {{ include "common.names.namespace" $ }}
spec:
  endpoints:
    - fqdn:
        hostname: brave-search.{{ include "common.names.namespace" $ }}.svc.cluster.local
        port: 8080
