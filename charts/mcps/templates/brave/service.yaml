---
apiVersion: v1
kind: Service
metadata:
  name: brave-search
  namespace: {{ include "common.names.namespace" $ }}
spec:
  selector:
    app: brave-search
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
