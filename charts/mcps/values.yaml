gatewayRef:
  name: core-gateway
  namespace: converse-gateway
  # Pin MCP routes to the Gateway's HTTPS listener (HTTPS-only); :80 is left to
  # the http→https redirect + the ACME solver.
  sectionName: api-https