gateway:
  # cert-manager ClusterIssuer name used to issue Gateway listener certificates.
  issuer: self-signed-ca

  # GatewayClass name referenced by the Gateway.
  className: core-gateway

  # Optional hostnames for the HTTPS listeners.
  # When empty, the `hostname:` field is omitted.
  apiHostname: ""
  serviceApiHostname: ""

