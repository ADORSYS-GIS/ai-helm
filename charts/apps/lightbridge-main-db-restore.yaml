# Restore cluster for lightbridge-main-db
# Usage: Add this file to Git during recovery, remove after cleanup
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: lightbridge-main-db-restore
  namespace: converse
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    description: "Restored from backup - switch traffic via lightbridge-main-db-primary service"
spec:
  instances: 2
  imageName: ghcr.io/cloudnative-pg/postgresql:18.1-system-trixie
  storage:
    storageClass: linode-block-storage
    size: 5Gi
  resources:
    limits:
      cpu: 300m
      memory: 1Gi
    requests:
      cpu: 200m
      memory: 500Mi
  postgresUID: 26
  postgresGID: 26
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: lightbridge-barman-store
        serverName: lightbridge-main-db-restore
  bootstrap:
    recovery:
      source: lightbridge-main-db-backup
  externalClusters:
    - name: lightbridge-main-db-backup
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: lightbridge-barman-store
          serverName: lightbridge-main-db