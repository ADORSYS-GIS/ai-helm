# The Docker/Rust source that used to live here moved to
# images/z-image-turbo-server/ when this model was retired from the home GPU and
# reintroduced as the `zimage` engine profile on the fleet (ADR-0100) — the image
# is live infrastructure and must not be deleted along with this legacy chart.
#
# The pattern stays so a stray local build directory is never packaged.
docker/
