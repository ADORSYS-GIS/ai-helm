# This chart includes a Rust server in ./docker/ with its own .gitignore.
# The root repo .gitignore already ignores charts/*/charts/ (dependency tarballs).
# Nothing chart-specific to add here — the docker/ build artifacts are handled
# by docker/.gitignore.


