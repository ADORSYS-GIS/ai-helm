apiVersion: v2
name: sonarqube-db
description: |
  CloudNativePG Cluster (sonarqube-db) backing SonarQube Community Build,
  plus its barman-cloud ObjectStore, ScheduledBackup, PodMonitor, the
  `sonarqube` Database and the role's basic-auth ExternalSecret. Leaf chart of
  the sonarqube App-of-Apps orchestrator (ADR-0065, mirrors lightbridge-db /
  ADR-0019). Namespace `sonarqube`.
type: application
version: 0.1.0
appVersion: "1.0.0"
