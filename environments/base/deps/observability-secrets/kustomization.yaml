# Base: observability stack ExternalSecrets (S3 creds + grafana admin/keycloak).
# Placeholder remoteRefs — see external-secrets.yaml. Store name patched per env.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - external-secrets.yaml
