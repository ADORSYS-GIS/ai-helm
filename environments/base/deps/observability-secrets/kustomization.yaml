# Base: observability stack ExternalSecrets (S3 creds + grafana admin/keycloak).
# Placeholder remoteRefs — see external-secrets.yaml. Store name patched per env.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - external-secrets.yaml
  # Restores intra-namespace ingress under the manual default-deny-ingress
  # baseline (Mimir mesh, Alloy→stores, Grafana→stores). See the file header.
  - allow-same-namespace.yaml
