# Base: app-scoped dependency CRs for the opencode-k8s-agent umbrella.
# The internal-CA trust bundle (ca.crt) the agent mounts to verify the
# api-internal gateway listener's TLS.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - certificate.yaml
