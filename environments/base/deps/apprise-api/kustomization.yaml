# Base: app-scoped dependency CRs for the apprise-api umbrella.
# The apprise-channels notification-config secret. Store patched per env.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - external-secret.yaml
