"""vLLM inference engine dashboards (GENERATED SOURCE).

Modules in this package generate the JSON shipped under
``charts/observability-dashboards/files/vllm/``. The JSON files are
regenerated from these modules — do **not** hand-edit them.

Regenerate with::

    make build
    # or
    uv run dashboards build
"""
