# Local state / secret scratch — never commit
.terraform/
*.tfstate
*.tfstate.*
.env
*.auto.tfvars

# NOTE: `.terraform.lock.hcl` is intentionally NOT ignored — it's committed so
# provider resolution is reproducible across runs (ADR-0124 reproducibility goal).
