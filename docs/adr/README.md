# Architecture Decision Records

This directory captures the **why** behind every meaningful architectural
choice in this repo. Implementation details (the **how**) live in the
broader `docs/` tree; ADRs link to them.

## What is an ADR

A short, dated record of a single decision: the context it was made in, the
options considered, what was chosen, and the consequences (good and bad). The
format is [Michael Nygard's original](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions).

ADRs are **immutable once accepted**. To change a decision, write a new ADR
that supersedes the old one — never edit history. Reading the chain shows
how thinking evolved.

## Index

| # | Title | Status | Date | Supersedes |
|---|---|---|---|---|
| [0001](./0001-record-architecture-decisions.md) | Record architecture decisions in this repo | Accepted | 2026-05-24 | — |
| [0002](./0002-replace-phoenix-with-tempo.md) | Replace Arize Phoenix with Grafana Tempo for LLM tracing | Accepted | 2026-05-24 | — |
| [0003](./0003-skip-opa-for-service-accounts.md) | Skip OPA / external metadata for service-account tokens via `azp` allowlist | Superseded by [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md) | 2026-05-24 | — |
| [0004](./0004-grafana-operator-external-mode.md) | Adopt grafana-operator in external mode for dashboards-as-code | Accepted | 2026-05-24 | — |
| [0005](./0005-per-user-attribution-via-authorino-headers.md) | Propagate per-user identity via Authorino response headers → Loki labels | Accepted | 2026-05-24 | — |
| [0006](./0006-multi-source-applicationset.md) | Migrate charts/apps to a multi-source ApplicationSet (List generator, no-dup) | Superseded by [0018](./0018-umbrella-apps-and-env-overlays.md) | 2026-05-24 | — |
| [0007](./0007-kc-token-go-cli.md) | Build `kc-token` as a single static Go binary + GH composite action | Superseded by [0009](./0009-ai-in-ci-via-keycloak-token-exchange.md) | 2026-05-24 | — |
| [0008](./0008-python-dashboard-generation.md) | Generate Grafana dashboards from Python | Accepted | 2026-05-24 | — |
| [0009](./0009-ai-in-ci-via-keycloak-token-exchange.md) | AI in CI via Keycloak OIDC token exchange (Python step, shared SA, fork-deny) | Accepted | 2026-05-24 | [0007](./0007-kc-token-go-cli.md) |
| [0010](./0010-argocd-image-updater-writeback-to-ai-gitops.md) | argocd-image-updater with git write-back to `ai-gitops` (PR + auto-merge, GitHub App auth, once per Application) | Superseded by [0013](./0013-defer-image-updater-writeback.md) | 2026-05-24 | — |
| [0011](./0011-oidc-downstream-headers.md) | Canonical `x-oidc-*` downstream header contract (supplements ADR-0005) | Accepted | 2026-05-24 | — |
| [0012](./0012-split-ai-models-applicationset.md) | Split `charts/ai-models` into 3 sub-charts + ApplicationSet (1 backends App + N per-model Apps) | Accepted | 2026-05-24 | — |
| [0013](./0013-defer-image-updater-writeback.md) | Defer argocd-image-updater write-back; manual chart-version bumps stay (supersedes ADR-0010) | Superseded by [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md) | 2026-05-24 | [0010](./0010-argocd-image-updater-writeback-to-ai-gitops.md) |
| [0014](./0014-split-librechart-and-opencode-wellknown.md) | Split `charts/librechart` into 3 leaf charts + add opencode `.well-known` endpoint | Accepted | 2026-05-24 | — |
| [0015](./0015-models-info-catalog-endpoint.md) | OpenRouter-shape `/v1/models/info` catalog endpoint for opencode (charts/ai-models-info) | Accepted | 2026-05-24 | — |
| [0016](./0016-fork-bjw-s-app-template-locally.md) | Fork bjw-s app-template + common locally as `bjw-template` / `bjw-common` (rewire 12 consumers) | Accepted | 2026-05-31 | — |
| [0017](./0017-home-remote-destination-invariant.md) | Workloads target the home-remote cluster, never in-cluster (render-time guard + `allowInCluster` escape hatch) | Accepted | 2026-05-31 | — |
| [0018](./0018-umbrella-apps-and-env-overlays.md) | Umbrella Applications (workload + app-scoped deps via kustomize) + per-env `environments/` overlays | Proposed | 2026-05-31 | [0006](./0006-multi-source-applicationset.md) |
| [0019](./0019-coder-app-of-apps-orchestrator.md) | Factor Coder into an App-of-Apps orchestrator (db + app as separate Applications) | Superseded by [0027](./0027-mcps-orchestrator-split-and-coder-removal.md) | 2026-06-01 | — |
| [0020](./0020-observability-app-of-apps-orchestrator.md) | Factor the observability stack into an App-of-Apps orchestrator (+ a secrets app) | Accepted | 2026-06-01 | — |
| [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md) | Burst control, budgeting & billing via dual-plane (internal/external) AuthConfigs | Proposed | 2026-06-04 | supersedes [0003](./0003-skip-opa-for-service-accounts.md) |
| [0022](./0022-self-hosted-gpu-model-federated-into-gateway.md) | Self-hosted GPU model (Qwen3-4B, KServe/Knative + vLLM + LMCache) federated into the gateway as a public-FQDN backend | Accepted | 2026-06-05 | exception to [0017](./0017-home-remote-destination-invariant.md) |
| [0023](./0023-grafana-stateless-no-pvc.md) | Grafana runs stateless (no PVC) — dashboards-as-code + provisioned datasources; persistence (if ever) goes to an external DB | Accepted | 2026-06-06 | builds on [0020](./0020-observability-app-of-apps-orchestrator.md) |
| [0024](./0024-right-size-observability-tiny.md) | Right-size observability to a tiny footprint (cluster + Envoy AI usage, no alerting) — fix Mimir PDB sync bug, drop dead components + Alertmanager | Accepted | 2026-06-06 | builds on [0020](./0020-observability-app-of-apps-orchestrator.md), [0005](./0005-per-user-attribution-via-authorino-headers.md) |
| [0025](./0025-linode-to-hetzner-cutover-domain-ai.md) | Cut over Linode → Hetzner; rename public domain `ai-v2.camer.digital` → `ai.camer.digital` (DNS-gated; Mongo data via script, Keycloak PG out of scope) | Accepted | 2026-06-06 | builds on [0017](./0017-home-remote-destination-invariant.md), [0018](./0018-umbrella-apps-and-env-overlays.md) |
| [0026](./0026-lightbridge-orchestrator-split.md) | Split lightbridge into an App-of-Apps orchestrator (secrets/db/app children); drop `opa` + `usage`, keep `api`+`mcp`; backups → Hetzner | Accepted | 2026-06-06 | builds on [0019](./0019-coder-app-of-apps-orchestrator.md); relates to [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0025](./0025-linode-to-hetzner-cutover-domain-ai.md) |
| [0027](./0027-mcps-orchestrator-split-and-coder-removal.md) | Split MCPs into per-MCP Applications (generic `charts/mcp` leaf + ApplicationSet, in-chart ExternalSecrets, + Refero); remove Coder | Accepted | 2026-06-06 | supersedes [0019](./0019-coder-app-of-apps-orchestrator.md); builds on [0012](./0012-split-ai-models-applicationset.md), [0017](./0017-home-remote-destination-invariant.md), [0018](./0018-umbrella-apps-and-env-overlays.md) |
| [0028](./0028-owned-hardware-model-pricing.md) | Price owned-hardware (self-hosted) models at cost-recovery, derived from a documented €/hour TCO (weighted strategy; replaces ADR-0022's flat $0) | Accepted | 2026-06-07 | amends [0022](./0022-self-hosted-gpu-model-federated-into-gateway.md) (pricing); builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0012](./0012-split-ai-models-applicationset.md) |
| [0029](./0029-self-hosted-model-plain-deployment.md) | Serve the self-hosted model as a plain Deployment (drop KServe/Knative) — always-on + Recreate on the dedicated GPU (kills cold starts + rollout deadlock) | Accepted | 2026-06-07 | supersedes [0022](./0022-self-hosted-gpu-model-federated-into-gateway.md) (serving mode only); builds on [0017](./0017-home-remote-destination-invariant.md) |
| [0030](./0030-merge-model-and-proxy-into-one-statefulset-bjw.md) | Co-locate the model + Caddy auth-proxy in ONE StatefulSet (proxy → model over localhost), rendered via bjw-template | Accepted | 2026-06-07 | refines [0029](./0029-self-hosted-model-plain-deployment.md); builds on [0016](./0016-fork-bjw-s-app-template-locally.md) |
| [0031](./0031-tag-based-deploys.md) | Tag-based deploys (`release-YYYY.MM.DD`), never `main`: self-ref `targetRevision`s + root pin an immutable tag (self-consistent commit); external first-party sources pinned to SHAs; `tools/release.sh` automates it | Superseded by [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md) | 2026-06-08 | retires the branch-deploy/flip-to-main plan; relates to [0010](./0010-argocd-image-updater-writeback-to-ai-gitops.md)/[0013](./0013-defer-image-updater-writeback.md) (ai-gitops absent); builds on [0017](./0017-home-remote-destination-invariant.md), [0018](./0018-umbrella-apps-and-env-overlays.md) |
| [0032](./0032-llama-cpp-engine-for-self-hosted-models.md) | llama.cpp (`llama-server`) as a 2nd self-hosted engine alongside vLLM: GGUF/Q4_K_M, native `--api-key` (no Caddy), `/v1`, `/health` probe — chosen for Qwen3.5-4B Q4 because vLLM's Qwen3.5 support is turbulent (`charts/model-serving-qwen3-5`, staged) | Accepted | 2026-06-08 | adds an engine to [0030](./0030-merge-model-and-proxy-into-one-statefulset-bjw.md)/[0029](./0029-self-hosted-model-plain-deployment.md); builds on [0022](./0022-self-hosted-gpu-model-federated-into-gateway.md), pricing [0028](./0028-owned-hardware-model-pricing.md) |
| [0033](./0033-relax-free-tier-burst-limits.md) | Relax free-tier burst rate limits (200 req/min, 200k tokens/min) and pro requests (400 req/min) — burst caps become backend guards, monthly budgets remain the spend backstop | Accepted | 2026-06-08 | amends [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md) |
| [0034](./0034-restore-streaming-timeouts-and-extproc-headroom.md) | Restore streaming-stability fixes lost when the magical-bohr rewrite diverged: explicit 600s AIGatewayRoute + per-model upstream timeouts (cloud models were cut at the 60s/15s defaults), ExtProc CPU headroom, OTel `debug` removal — re-implemented values-driven | Accepted | 2026-06-09 | re-implements pre-cutover `20d8f4f` + the "faster envoy responses" series; builds on [0012](./0012-split-ai-models-applicationset.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0022](./0022-self-hosted-gpu-model-federated-into-gateway.md) |
| [0035](./0035-per-person-monthly-budget-and-free-50.md) | Key the monthly spend budget on `x-account-id` (per-person) instead of `x-org-id` (shared org bucket) so colleagues don't contend for one pool; raise the free tier $30 → $50 | Accepted | 2026-06-09 | amends [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md); relates to [0033](./0033-relax-free-tier-burst-limits.md) |
| [0036](./0036-remove-apprise-notification-path.md) | Remove the redundant Grafana apprise path (`grafana-apprise-adapter` + external-alertmanager override → Grafana native); keep a LEAN stateless `apprise-api` (drop the `apprise-channels` mount that hung it 0/1) solely as opencode-k8s-agent's `/notify` gateway — the agent has no built-in notifier | Accepted | 2026-06-09 | relates to [0020](./0020-observability-app-of-apps-orchestrator.md), [0024](./0024-right-size-observability-tiny.md) |
| [0037](./0037-opencode-agent-internal-sa-token.md) | opencode-k8s-agent auth → the INTERNAL plane via its own projected SA token (ADR-0021 one-time-job path): drops Keycloak/OAuth2 + the apiKey secret; internal `core-gateway-internal` endpoint + internal-CA trust like LibreChat; opencode.json static-bearer change via agent-repo PR | Accepted | 2026-06-09 | builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0018](./0018-umbrella-apps-and-env-overlays.md); relates to [0036](./0036-remove-apprise-notification-path.md) |
| [0038](./0038-mcp-oauth-protected-resource-metadata.md) | MCP OAuth discovery (RFC 9728) via native AIEG `MCPRoute.securityPolicy.oauth`: gateway-served PRM + AS metadata + 401 `resource_metadata` challenge per `/mcp/*`; Envoy-native JWT replaces Authorino on MCP routes (x-oidc-* restored via claimToHeaders); plus a non-spec path-appended PRM alias via DirectResponse | Accepted | 2026-06-10 | builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0027](./0027-mcps-orchestrator-split-and-coder-removal.md); relates to [0011](./0011-oidc-downstream-headers.md) |
| [0039](./0039-mcp-external-backend-tls-envoypatchpolicy.md) | Repair external MCP backend upstream TLS via `EnvoyPatchPolicy` (charts/core-gateway): AIEG stamps a `dummy.transport_socket` (empty SNI) on MCP backend clusters that `BackendTLSPolicy`/`Backend.spec.tls` never reach → external HTTPS handshake fails; patch injects a real TLS socket w/ SNI + system-CA. RSA upstreams only (firecrawl, refero); context7's ECDSA cert is BoringSSL-rejected → self-hosted instead | Superseded by [0040](./0040-external-mcps-via-caddy-normalizing-proxy.md) | 2026-06-10 | builds on [0038](./0038-mcp-oauth-protected-resource-metadata.md), [0027](./0027-mcps-orchestrator-split-and-coder-removal.md) |
| [0040](./0040-external-mcps-via-caddy-normalizing-proxy.md) | External hosted MCPs via per-MCP in-cluster **Caddy normalizing proxies** (`charts/mcp` `mode: proxiedExternal`): Caddy does the upstream TLS (Go TLS handles context7's ECDSA cert), injects the credential, and rewrites refero's mislabeled `text/event-stream`→`application/json` (AIEG #2218 workaround). Turns external MCPs into reliable in-cluster plain-HTTP backends; **removes** the ADR-0039 EnvoyPatchPolicy. Brings context7 back; off-the-shelf caddy image (no custom build) | Accepted | 2026-06-11 | supersedes [0039](./0039-mcp-external-backend-tls-envoypatchpolicy.md); builds on [0027](./0027-mcps-orchestrator-split-and-coder-removal.md), [0030](./0030-merge-model-and-proxy-into-one-statefulset-bjw.md) |
| [0041](./0041-firecrawl-protocol-version-rewrite-nginx-engine.md) | **openresty proxy engine** for firecrawl (`charts/mcp` `proxy.engine: openresty` + `pinRequestProtocolVersion`): firecrawl frames its `initialize` SSE with an empty leading event for `protocolVersion 2025-11-25` (the MCP SDK/opencode default); the AIEG v0.7.0 mcpproxy reads it as `MCP message is not a response: <nil>` → 500. Neither Caddy nor stock nginx can rewrite a REQUEST body, so openresty (nginx+Lua) pins the request `protocolVersion` down to `2025-06-18` (clean framing). INTERIM pending AIEG #2219; reverted the wrong `statelessUpstream` synthesis and the wrong `nginx`/`rewriteResponseProtocolVersion` | Accepted | 2026-06-11 | extends [0040](./0040-external-mcps-via-caddy-normalizing-proxy.md) |
| [0042](./0042-opencode-wellknown-mcp-catalog.md) | Ship a curated **MCP catalog in the opencode `.well-known`** (`charts/librechat-opencode-wellknown` `wellKnown.config.mcp`): the gateway `/mcp/*` remotes (brave/context7/refero/firecrawl/terraform) + a local `chrome-devtools` server, pushed org-wide so every user shares one config. Remotes reuse one `opencode-cli` OAuth anchor. Lean-by-default enablement policy: only `brave` + `context7` on; specialist remotes + `chrome-devtools` present-but-disabled (one-flag opt-in) | Accepted | 2026-06-12 | extends [0014](./0014-split-librechart-and-opencode-wellknown.md); points at [0038](./0038-mcp-oauth-protected-resource-metadata.md) |
| [0043](./0043-one-skills-system-in-the-opencode-wellknown.md) | **One skills system in the opencode `.well-known`** — decline `obra/superpowers`. It plays the same role as the already-shipped `opencode-skills-collection` (both are skills systems); since pushed plugins are mandatory-on (no per-user `enabled` flag, unlike MCP servers), stacking overlapping-role plugins doubles the system-prompt cost + imposition. Principle: one skills system at a time; check a candidate plugin doesn't duplicate an existing role. Generalizes the [0042](./0042-opencode-wellknown-mcp-catalog.md) org-push filter with a role-overlap guard | Accepted | 2026-06-12 | relates to [0042](./0042-opencode-wellknown-mcp-catalog.md), [0014](./0014-split-librechart-and-opencode-wellknown.md) |
| [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md) | **Role subagents + permission-scoped tools** in the opencode well-known: two decoupled axes — MCP `enabled` = connectivity, per-agent `permission` = access. A global deny-baseline keeps the PRIMARY agent lean (no MCP tools); each role is a `mode: subagent` whitelist (web-search→brave read-only; doc-research→context7 + writes only `docs/**`; iac→context7+terraform + bash guardrails). Connects terraform; refines [0042](./0042-opencode-wellknown-mcp-catalog.md) (brave/context7 no longer on the primary, now behind subagents). Runtime enforcement to be live-validated | Accepted | 2026-06-12 | refines [0042](./0042-opencode-wellknown-mcp-catalog.md); builds on [0014](./0014-split-librechart-and-opencode-wellknown.md), [0038](./0038-mcp-oauth-protected-resource-metadata.md) |
| [0045](./0045-scrape-first-dashboard-sourcing.md) | **Scrape-first dashboard sourcing**: never import a board before its metrics verifiably exist in Mimir (the live audit found 6 of 13 imported gnetId boards dead from missing scrapes, not bad dashboards — Mimir/Loki don't even scrape themselves); prefer scrape-enablement that revives an existing board over new dashboard work; community boards only with API-verified gnetId/revision + metric-name check (three phantom/incompatible imports prior); dashboards-as-code (ADR-0008) only where no community equivalent exists (per-user usage, ratelimit, Authorino — verified zero results); no boards for unreachable data (home-GPU model serving needs a remote-write ADR first) | Accepted | 2026-06-12 | uses [0008](./0008-python-dashboard-generation.md); evidence: [observability-dashboard-research.md](../patterns/observability-dashboard-research.md); decided alongside [0046](./0046-per-user-attribution-otlp-envelope-repair.md) |
| [0046](./0046-per-user-attribution-otlp-envelope-repair.md) | **Per-user attribution repair — flatten the OTLP access-log envelope at Alloy**: Envoy's OTel sink emits the access-log `format.json` fields as OTLP *attributes*, so Alloy stored `{"attributes":{...},"resources":{...}}` — ADR-0005's top-level label promotion never fired, the stream landed as `service_name="unknown_service"`, and the per-user board was empty since rollout (data itself intact: 21 users with real token totals re-queried via the nested form). Fix: Alloy `stage.match` on the `otel_envoy_accesslog` marker → flatten the line to the attributes object (`stage.output`) → promote `user_id`/`azp`/`model` labels (`-`→empty by exact match — UUIDs contain `-`) → pin `service_name=envoy-ai-gateway` (`stage.static_labels`; the Envoy sink also sets the `service.name` resource). Query contract: anchor on that `service_name`; numeric fields are strings, absent ones `-` → every `unwrap` needs `\| __error__=""` | Accepted | 2026-06-12 | supplements [0005](./0005-per-user-attribution-via-authorino-headers.md); uses [0011](./0011-oidc-downstream-headers.md), [0008](./0008-python-dashboard-generation.md); evidence: [observability-dashboard-research.md](../patterns/observability-dashboard-research.md) |
| [0047](./0047-github-oidc-repo-binding-for-ci.md) | **Bind GitHub orgs to accounts via OIDC for CI gateway access**: CI runners authenticate to the gateway with their raw GitHub Actions OIDC token (no shared secret, no per-runner registration). A GitHub App (auth-only: `metadata:read` + install events) webhooks the new [`lightbridge-repo-auth`](https://github.com/ADORSYS-GIS/lightbridge-repo-auth) service, which records `account_id ↔ repository_owner_id` (GitHub's numeric, server-set, immutable org id, captured from the webhook payload). At request time Authorino's `main` AuthConfig validates the GitHub JWT (issuer `token.actions.githubusercontent.com`) and — `when`-gated to that issuer — calls `/v1/resolve` (HTTP metadata, `X-Internal-Token`) to confirm the owner is bound to a claimed/active Source and stamp `x-account-id`/`x-billing-plan`. Enforcement is Authorino, NOT a Keycloak SPI (Keycloak 26.6 `jwt-bearer` needs a pre-linked user — fights emergent CI `sub`s). Keycloak path untouched. | Proposed | 2026-06-14 | builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0011](./0011-oidc-downstream-headers.md), [0035](./0035-per-person-monthly-budget-and-free-50.md); service: [lightbridge-repo-auth](https://github.com/ADORSYS-GIS/lightbridge-repo-auth) |
| [0048](./0048-global-browser-plugin-and-per-agent-tool-injection.md) | **Global browser plugin + lean frontend primary; per-agent tool injection is real**: ship maintainer-authored `@vymalo/opencode-browser` org-wide (all three groups `page`+`control`+`debug` = 33 tools incl. `browser_eval`; ⚠️ `debug` re-enabled 2026-06-14 by maintainer request — header-note amendment); **remove the `chrome-devtools` MCP**; promote `frontend` to the org-wide default **primary** (`default_agent`) but keep it LEAN (edit + JS toolchain), delegating the heavy tools to subagents — `@browser` (the 33 `browser_*`), `@design` (refero), docs→`@doc-research`. **Model rule (no-risk):** pin a multimodal model ONLY on agents whose tools return images (`@browser`/`@design` → `adorsys-frontend`); EVERY other agent (primary + all roles) carries no `model` and inherits the session model — drops the ADR-0044 per-role cost-lean pins. One source only — don't also connect the dual package's MCP form. **Corrects ADR-0044's token rationale**: opencode DOES scope the *injected* tool set per agent — `request.ts resolveTools`→`Permission.disabled` drops a tool from `tools`/`activeTools` on a string-form `*`-deny (built-in/MCP/plugin alike; agent `allow` wins via `findLast`), so the lean primary's default context never carries the 33 `browser_*` schemas | Accepted | 2026-06-14 | refines/overrides in part [0042](./0042-opencode-wellknown-mcp-catalog.md) (removes chrome-devtools), [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md) (corrects token model, rewrites the `@frontend` role + model tiering); builds on [0014](./0014-split-librechart-and-opencode-wellknown.md) |
| [0050](./0050-opencode-toolbelt-0.8.0-and-interactive-browser-feedback.md) | **Bump the @vymalo OpenCode Toolbelt to 0.8.0 + register the `interactive` browser-feedback group**: move the four `@vymalo/*` plugins `0.7.3`→`0.8.0` (the rebranded "OpenCode Toolbelt" suite; transparent browser improvements — multi-client broker, full-page capture, `ws`/Node transport, no stdout flooding). Register the new opt-in **`interactive`** browser group (its lone tool `browser_request_feedback` asks the human ON THE PAGE — confirm/choose/point/element/region/comment — and blocks ≤300s) → `[page, control, debug, interactive]` = **34 tools**; no permission change (the `browser_*` deny-baseline + `@browser` allow already scope it to `@browser` only). Far tamer than `debug` (no arbitrary JS); fits the design loop. Also corrects stale `0.6.x` minimum-version citations (`responseApi`/`modelsInfoOverwrite` are `≥0.7.1`). | Accepted | 2026-06-14 | refines [0048](./0048-global-browser-plugin-and-per-agent-tool-injection.md); builds on [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md), [0042](./0042-opencode-wellknown-mcp-catalog.md), [0014](./0014-split-librechart-and-opencode-wellknown.md) |
| [0051](./0051-unify-cost-metadata-key-to-llm-custom-total-cost.md) | **Unify pricing CEL to `llm_custom_total_cost`; retire per-model metadata keys**: a prior undocumented workaround (per-model `cost_<model>` keys via `metadataKeySafe`) for a multi-route metadata deduplication bug in an older AIEG/Envoy version caused `llm_custom_total_cost` to always be `"-"` in access-log streams → all Grafana cost panels "No data" since the workaround was introduced. Current Envoy fixes the deduplication issue; all models now write to the standard key; `metadataKeySafe` helper removed | Accepted | 2026-06-15 | builds on [0028](./0028-owned-hardware-model-pricing.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md); discovered via [0046](./0046-per-user-attribution-otlp-envelope-repair.md) dashboard repair |
| [0049](./0049-source-claim-operator-cli-not-self-serve.md) | **Claim Sources via an operator CLI, not a self-serve UI (for now)**: the ADR-0047 "claim" (link a Source to a billing account) has no public path — we onboard ≤~5 first-party orgs by hand, so a self-serve onboarding UI is disproportionate and a public claim endpoint is needless attack surface. The admin API (`/v1/admin/claim`, `/v1/admin/sources`) stays **ClusterIP-only + X-Internal-Token-guarded** (dumb store, no identity policy); operators use the **`repo-auth-ctl`** CLI to claim. Documents (but does NOT build) the future self-serve design: a dashboard does GitHub OAuth + verifies the claimer **administers** the install via `GET /user/installations` (+ signed `state`) before calling the same dumb endpoint — `installation_id` from the redirect is not proof on its own. Build it only when onboarding external customers. | Accepted | 2026-06-14 | builds on [0047](./0047-github-oidc-repo-binding-for-ci.md) |
| [0050](./0050-kimi-k2.7-code-supersedes-k2.6-as-frontend-pro-backing.md) | **Kimi K2.7 Code supersedes Kimi K2.6 as the `adorsys-frontend-pro` backing**: K2.7 Code (DeepInfra `moonshotai/Kimi-K2.7-Code`, $0.74/$0.15/$3.50, 262k ctx, multimodal) strictly dominates K2.6 ($0.75/$0.15/$3.50) — same price, still multimodal, +21.8% Kimi Code Bench v2 / +11% Program Bench, ~30% fewer reasoning tokens. Repoint `adorsys-frontend-pro` + retire the standalone `kimi-k2.6` catalog model (replaced by `kimi-k2.7-code`); budget unchanged ($15/$50). A pure backing swap in the ADR-0044 spirit (invisible to the well-known; `-pro` is user-selectable, not agent-pinned). **GLM-5.2 evaluated and declined** — equal intelligence-per-$ to GLM-5.1 (premium ceiling, not better value); not added. | Accepted | 2026-06-18 | supersedes the K2.6 backing in [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md) |
| [0053](./0053-vanity-domain-redirects-kivoyo-to-camer-digital.md) | **Redirect the `kivoyo.com` vanity hosts to their `camer.digital` canonicals**: we own kivoyo.com and its DNS already points at our LBs, so redirect (temporary, path+query preserving) `api.ai.kivoyo.com → api.ai.camer.digital` and `ai.kivoyo.com → ai.camer.digital` rather than running two front-doors (which would fork TLS/cookies/OIDC redirect URIs/attribution). API host: a new reusable `gateway.redirectHosts` list on `charts/core-gateway` (per-entry HTTPS listener + ACME cert reusing the in-chart issuer + a `RequestRedirect` HTTPRoute on `:80`+the per-host listener), `307` (method-preserving). Web host: a Traefik `redirectRegex` Middleware + dedicated Ingress as `librechat.rawResources` on `charts/librechat-app`, `302`. Both certs via existing ACME (no DNS token); ACME-safe by exact-challenge-path precedence. Temporary so kivoyo can be promoted later without cached-redirect lock-in. | Accepted | 2026-06-18 | builds on [0014](./0014-split-librechart-and-opencode-wellknown.md), [0017](./0017-home-remote-destination-invariant.md) |
| [0054](./0054-adopt-k3s-bundled-metrics-server.md) | **Adopt the k3s-bundled metrics-server instead of shipping our own**: the GitOps `metrics-server` (`aii-metrics-server`, kubernetes-sigs chart, 2 replicas) collided with k3s's bundled metrics-server addon on the shared `Deployment`/`Service` name `metrics-server` in `kube-system` — k3s re-applied its pod (label `k8s-app` only), our Service selector (`app.kubernetes.io/{name,instance}`) stopped matching → empty endpointslice → `v1beta1.metrics.k8s.io` `MissingEndpoints` → `kubectl top`/HPA dead on every sync. The k3s one is healthy (Ready, `/readyz` ok, scrapes kubelets); the heavy ADR-0015 fix (`--disable metrics-server` + CP node `-replace`) hadn't held across restores. So **drop our app** (remove the `charts/apps` entry); ArgoCD prunes our objects and the k3s addon re-owns the Service+APIService → metrics recover, no node reprovision. Trade-off: lose GitOps control of metrics-server config (args/HA/PDB now whatever k3s ships). Reverses hetzner-k8s ADR-0015 intent for metrics-server specifically. | Accepted | 2026-06-19 | reverses (cross-repo) hetzner-k8s ADR-0015 for metrics-server; supersedes the §4 remediation in `docs/2026-06-07-observability-datasource-audit.md` |
| [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md) | **OCI-published charts + argocd-image-updater write-back to a private `ai-helm-values` repo**: retire tag-based deploys for continuous delivery — charts publish to `oci://ghcr.io/adorsys-gis/charts` on merge to main (auto-semver, derived at publish time so no commit-back loop; local subcharts vendored), child Applications float on a semver range (immutability **deliberately abandoned** — "n:n"), and image tags flow via direct-commit cosign-gated git write-back to the private `ai-helm-values` repo (which also absorbs the migrated `environments/` overlays — values+image-tags scope; root `ai-apps-v2` stays in home-os). Generalizes the live `lightbridge-code-intelligence` pattern. `tools/release.sh` retired. New cred surface: GitHub App write + ArgoCD read on the private values repo. | Accepted | 2026-06-22 | supersedes [0013](./0013-defer-image-updater-writeback.md), [0031](./0031-tag-based-deploys.md); amends [0018](./0018-umbrella-apps-and-env-overlays.md); builds on [0017](./0017-home-remote-destination-invariant.md), realises [0010](./0010-argocd-image-updater-writeback-to-ai-gitops.md) |
| [0056](./0056-workload-values-in-ai-helm-values.md) | **Move workload Helm values out of `charts/apps` into `ai-helm-values`**: ADR-0055 moved image tags to the values repo but the bulk of each app's config stayed inline in `charts/apps/values.yaml` (1689 lines, dominated by `security-policies`' ~405-line AuthConfig in a *public* repo). Move the three heaviest `valuesObject` blocks (security-policies, eg, opencode-k8s-agent ≈ 590 lines) to `environments/<env>/values/<app>.yaml` in `ai-helm-values`, read via a `$values` ref. Opt-in with one flag — `valuesFromRepo: true` — that the template expands (inject `valueFiles` + `ignoreMissingValueFiles` + append the ref source); the inline block is deleted. A `render-check.yml` in the values repo YAML-validates + `helm template`s the OCI charts (because `ignoreMissingValueFiles` would otherwise silently mask a bad file → drop the AuthConfig). Cut over values-repo-first. Trade-off: local render coverage now spans two repos. Phase 2 then moved the remaining flat apps (apprise-api, aieg, core-gateway, lightbridge-repo-auth, mongodb-backup, converse-ui); phase 3 the `observability` (8 children incl. the Alloy River config; shared YAML anchors inlined to literals + the `global:` block dropped) and `lightbridge` App-of-Apps orchestrators (same per-child `valuesFromRepo` injection added to their templates). **Every inline `valuesObject` with real config is now in the values repo** (ai-models/librechart/mcps have none — leaf-chart config). | Accepted | 2026-06-23 | amends [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md), [0018](./0018-umbrella-apps-and-env-overlays.md); touches the [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md) AuthConfig |
| [0057](./0057-genericize-chart-defaults-deployment-specifics-to-values-repo.md) | **Genericize a workload chart's own `values.yaml`; deployment specifics → `ai-helm-values`**: ADR-0056 moved the `charts/apps` `valuesObject`, but the leaf `lightbridge-code-intelligence` chart still baked this-deployment literals as chart "defaults" — OIDC issuer/audience/client, public hostnames + ingress class/issuer/TLS, the `ssegning-aws` store + SM paths/properties, the CNPG `DATABASE_URL`, the gateway URLs + embeddings/LLM models, the GitHub App handle, and the ~150-line reviewer prompt (which was **duplicated** in the chart AND the override and had **diverged**). Make the chart ship only environment-agnostic base values (structure/resources/probes/security/ports/sync-waves/product-names/feature-flags/guards), empty out every deployment literal, and supply them per-env from `environments/<env>/values/<app>.yaml` via the ADR-0056 `$values` ref. Ingress now defaults `enabled: false` (no broken empty-host render); `DATABASE_URL` `value` moves but `dependsOn` stays (keeps `$(DB_PASSWORD)`); the stale chart prompt is deleted (values repo is sole source per the app repo's ADR-0037, fail-closed). **Render-neutral by construction** — proven by a normalized per-resource diff of the merged render before/after (26 resources byte-identical, only emission order differs). Trade-offs: defaults-only render is a non-deployable (intentionally fail-closed) skeleton; real-render verification needs both repos; missing override keys aren't schema-caught. | Accepted | 2026-06-23 | amends [0056](./0056-workload-values-in-ai-helm-values.md); relates to [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md) + the app repo's ADR-0037 |
| [0058](./0058-precompute-gateway-usage-metrics-to-mimir.md) | **Precompute AI Gateway usage (cost/tokens/requests) as Mimir metrics, not live Loki log-scans**: the cost dashboards `unwrap`-scanned 30 days of Envoy access logs from Loki, whose chunks live in rate-limited Hetzner Object Storage (`SlowDown`) — a 30d+5m-refresh board hammered the store, saturated Loki, and showed "No data" everywhere. Two stopgaps shipped (Loki chunk+results cache; boards tamed to 7d + auto-refresh off), but month-of-logs scanning a rate-limited store is fundamentally non-interactive. Fix: emit `gen_ai_usage_cost_micro_usd` / `gen_ai_usage_tokens` / `gen_ai_requests` counters at INGEST via Alloy `loki.process` `stage.metrics` (inheriting the ADR-0046 attribution labels), shipped to Mimir by the existing self-ServiceMonitor scrape — then repoint the dashboards to PromQL on Mimir and restore the 30d default. **Rejected the Loki ruler** (recording rules) because it would re-query Loki/S3 on a timer, re-creating the throttle. Part A = emit+verify (dashboard-neutral); Part B = repoint. Forward-only history. Phase 3 (gamified multi-viz app scoreboard — heatmap/histogram/candlestick/flame-graph/dashboard-list hub/traces/alert-list/text+news) captured, not built. | Accepted | 2026-06-25 | builds on [0005](./0005-per-user-attribution-via-authorino-headers.md), [0046](./0046-per-user-attribution-otlp-envelope-repair.md), [0008](./0008-python-dashboard-generation.md), [0028](./0028-owned-hardware-model-pricing.md) |
| [0059](./0059-grafana-unified-alerting-to-discord.md) | **Grafana unified alerting → Discord, provisioned as grafana-operator CRs**: we had no alerting (apprise path removed ADR-0036, Mimir Alertmanager disabled + its orphaned PVC deleted). Provision Grafana-native unified alerting entirely as grafana-operator CRs in `charts/observability-dashboards` (so it survives stateless-Grafana rolls, ADR-0023, like the dashboards): a `GrafanaContactPoint` `discord` (webhook injected via `valuesFrom` from an ESO secret — `ssegning-aws` property `grafana_discord_webhook_url`, never in git), a `GrafanaNotificationPolicy` (all → Discord), and 5 `GrafanaAlertRuleGroup`s (9 rules: gateway no-traffic / 5xx / p95, daily+monthly cost guardrails on the ADR-0058 metric, component-down, pod-crashloop, node-not-ready/mem). Rules use a compact values shape expanded into Grafana's A=query/B=reduce/C=threshold model (Mimir + Loki). Rejected the grafana chart `alerting:` files (splits from the CR model) and Mimir Alertmanager+ruler (just disabled; can't query Loki). ⚠️ Delivery needs the webhook property populated; thresholds are first-pass (tune live). | Accepted | 2026-06-25 | builds on [0023](./0023-grafana-stateless-no-pvc.md), [0020](./0020-observability-app-of-apps-orchestrator.md), [0058](./0058-precompute-gateway-usage-metrics-to-mimir.md); relates to [0036](./0036-remove-apprise-notification-path.md) |
| [0060](./0060-gamified-app-scoreboard.md) | **Gamified "App Scoreboard" dashboard (Phase 3 of cost observability)**: build the multi-panel scoreboard ADR-0058 deferred — one generated `GrafanaDashboard` (`envoy-ai-gateway-scoreboard`, ADR-0008 source, shipped via `charts/observability-dashboards` so it survives stateless-Grafana rolls) over the **same** Mimir metrics + Tempo traces + ADR-0059 alert state, using viz the cost boards don't: budget-burn **gauge** (% of an editable `$budget` textbox var, default $3000/mo), spend/requests/actors **stats**, top-actor/top-model **bargauge** leaderboards, per-actor spend **histogram**, plan-share **pie**, hourly token-intensity **heatmap**, daily-spend **timeseries**, Tempo **traces**, unified-alerting **alertlist**, a tag-`ai-gateway` **dashlist** hub, and an AI-governance **news** + **text** panel (the news feed is served same-origin via a Caddy proxy — see ADR-0061; the original "needs `github.com` egress" note was corrected). **candlestick + flame-graph deliberately deferred** — they need intra-day OHLC tick / Pyroscope profile-frame data we don't collect; synthesising them would imply data we don't have. | Accepted | 2026-06-26 | builds on [0058](./0058-precompute-gateway-usage-metrics-to-mimir.md), [0059](./0059-grafana-unified-alerting-to-discord.md), [0008](./0008-python-dashboard-generation.md), [0046](./0046-per-user-attribution-otlp-envelope-repair.md), [0023](./0023-grafana-stateless-no-pvc.md) |
| [0061](./0061-same-origin-proxy-for-grafana-news-feed.md) | **Generic same-origin Caddy proxy (`same-origin-proxy`)**: the ADR-0060 governance *news* panel showed "Error loading RSS feed … CORS". Root cause — Grafana's news panel fetches its feed **client-side**, and GitHub's `.atom` sends no `Access-Control-Allow-Origin` header → the browser blocks it (NOT a pod-egress issue; the pod reaches github.com fine, so the `github.com` egress first added to Grafana was useless and was removed). Fix: a **reusable** off-the-shelf **Caddy** chart (`charts/same-origin-proxy`, reusing the ADR-0040 pattern) that serves external resources **same-origin** under an app's host — a `routes[]` list (path → upstream), one `handle` block + Ingress path each, exposed via a second Traefik Ingress; 1st route = the governance feed at `/_governance.atom` under Grafana. `rewrite *` pins each route to one upstream + an **in-chart** `CiliumNetworkPolicy` whose egress FQDNs are **derived from the routes** (ingress←traefik) → **not an open proxy**. Reuses the host's TLS (no new host/cert). | Accepted | 2026-06-26 | amends [0060](./0060-gamified-app-scoreboard.md); relates to [0040](./0040-external-mcps-via-caddy-normalizing-proxy.md) |
| [0052](./0052-source-qualified-missing-claim-sentinels.md) | **Self-diagnosing identity attribution — source-qualified sentinels for absent claims**: never resolve an absent `x-oidc-*` value to empty (Loki drops empty labels → failures go invisible, as they had since rollout). Resolve to a descriptive, source-qualified sentinel naming *what* and *where*: `missing:<source>:<claim>` set by Authorino (`source` ∈ keycloak/github via the GitHub-only `repository` claim; librechat/service on the internal plane) = token lacked the claim; `unstamped:<field>` set by Alloy (`^(-\|<nil>)$`) = no header arrived at all. `has()`-guards extended to every scalar `x-oidc-*` (beyond ADR-0047's email/name); internal plane gains `x-oidc-name` (+ LibreChat forwards `X-LibreChat-Name`, services self-name); board excludes `(missing\|unstamped):.*` from per-user human panels but keeps them visible in Overall/Top-15. Root cause was upstream: `lightbridge-api-key` self-service keys are Keycloak token-exchange **lightweight access tokens** carrying no profile/email scope (Keycloak-side fix, out of repo). Rate-limit descriptors (`x-account-id`/`x-org-id`/`x-billing-plan`) deliberately NOT yet guarded. Negligible Authorino cost (compiled CEL, no I/O). | Accepted | 2026-06-18 | builds on [0011](./0011-oidc-downstream-headers.md), [0047](./0047-github-oidc-repo-binding-for-ci.md), [0046](./0046-per-user-attribution-otlp-envelope-repair.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md) |
| [0062](./0062-grafana-llm-assistant-via-internal-gateway.md) | **Grafana AI assistant (grafana-llm-app) on our internal AI-gateway plane**: enable the official LLM plugin for in-Grafana AI (panel explain / query + alert gen / chat) but route all of it through our own Envoy AI Gateway for governance + per-account cost attribution. Auth must use the **internal** plane (`core-gateway-internal…svc`, static apiKey) — the external host needs a short-lived Keycloak JWT a static-bearer plugin can't hold. A **dedicated** key (Secret `internal-key-grafana`, new ssegning-aws property `grafana_llm_gateway_key`) gives a deterministic `x-account-id` bucket (a shared value would attribute nondeterministically). Internal listener is HTTPS/`self-signed-ca`, so a throwaway CA leaf's `ca.crt` is mounted into `/etc/ssl/certs` (Go appends → internal CA trusted, public roots kept); Cilium egress to `envoy-gateway-system:443` opened. 100% declarative (Grafana is stateless, ADR-0023) → survives pod rolls; the key env is `optional: true` so a not-yet-synced key degrades only the assistant. Upstream chart = config-only, so it all lives in `ai-helm-values` (ADR-0056); mcp-grafana/ai-marketplace out of scope. ⚠️ values-repo-first: provision `grafana_llm_gateway_key` then re-roll the pod. | Superseded by [0076](./0076-remove-grafana-llm-assistant.md) | 2026-06-24 | builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0023](./0023-grafana-stateless-no-pvc.md), [0056](./0056-workload-values-in-ai-helm-values.md) |
| [0063](./0063-grafana-readonly-keycloak-datasource.md) | **Grafana read-only Keycloak datasource for `user_id` → identity attribution**: the per-user cost boards key on the gateway `user_id`, which for a Keycloak token is the `sub` (a UUID); when the token lacks email/name claims the UUID is all that survives, so "who is this?" needs a manual Admin-API lookup. Fix: a **read-only Postgres `GrafanaDatasource`** (`uid: keycloak`, the `-ro` replica) onto the Keycloak CNPG DB — same shape as `lci-postgres` — joined to the Mimir per-user metrics by a generated `user-directory` dashboard (Mixed-datasource `joinByField` outer on `user_id`; empty Name ⇒ non-human CI/internal subject). Because it's the **auth DB**, the `grafana_ro` role is **least-privilege** — SELECT on user+token tables ONLY (NOT `pg_read_all_data`: no `credential` hashes, client secrets, federated tokens). Cross-repo: role + ESO secret + idempotent GRANT Job live in **home-os** `keycloak-ha` (it owns the cluster); datasource + egress netpol + secret-copy live in **ai-helm-values**; dashboard in **ai-helm**. Realm filtered by literal `realm_id` (role can't read `realm`). ⚠️ out-of-band-secret-first: add `keycloak_grafana_ro_db_password` to `prod/meta/test-app` before sync. | Accepted | 2026-06-26 | builds on [0011](./0011-oidc-downstream-headers.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0046](./0046-per-user-attribution-otlp-envelope-repair.md), [0058](./0058-precompute-gateway-usage-metrics-to-mimir.md), [0008](./0008-python-dashboard-generation.md) |
| [0064](./0064-keycloak-sessions-and-grants-visibility.md) | **Keycloak sessions & grants visibility (extends ADR-0063)**: surface the "token objects" — which grants are in use / revoked / used which budget — bounded by what Keycloak actually stores. Reality (verified live, KC 26.6.1): access tokens are **stateless JWTs (never in the DB)**; KC 26 **persistent-sessions** keeps **both** online and offline sessions in `offline_{user,client}_session`, told apart by **`offline_flag`** (`'1'`=offline grant, `'0'`=online login) — so every grant query filters `offline_flag='1'` (127 offline client-sessions live, mostly `opencode-cli`) or it miscounts online logins. Revocation **deletes the row** (no tombstone → "active vs gone", not an enumerable revoked-list); `revoked_token` is the live revoke list. Budget carries `user_id`/`azp` labels but **no `jti`** → per-token budget is unavailable AND ill-defined (one offline token mints many access tokens). Decision: a generated **`sessions-grants` dashboard** (per-(user,client) detail with idle-days, by-client bargauge, two Mixed `joinByField` tables joining grant counts to per-`azp`/per-user spend = "which channel used which budget"), plus a **column-level** `GRANT SELECT (id, client_id, name) ON client` (resolve client UUID→name without exposing `client.secret`). Rejected: promoting `jti` to a metric label (unbounded cardinality). | Accepted | 2026-06-26 | builds on [0063](./0063-grafana-readonly-keycloak-datasource.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0035](./0035-per-person-monthly-budget-and-free-50.md), [0058](./0058-precompute-gateway-usage-metrics-to-mimir.md) |
| [0065](./0065-sonarqube-community-build-with-branch-plugin.md) | **SonarQube Community Build + community-branch-plugin (REJECTED, never implemented)**: proposed self-hosting free SonarQube with the third-party branch-plugin for inline GitHub PR decoration + Keycloak SAML + CNPG. **⚠️ Rejected — the team judged SonarQube a heavyweight "dinosaur" (JVM server + Postgres + Elasticsearch + node sysctl + an unofficial version-lockstep plugin just to decorate PRs) and chose [opengrep](./0066-opengrep-ci-native-sast.md) instead (ADR-0066).** | Rejected | 2026-06-27 | relates to [0066](./0066-opengrep-ci-native-sast.md) |
| [0066](./0066-opengrep-ci-native-sast.md) | **Adopt opengrep for CI-native SAST** instead of a code-quality server (reverses ADR-0065): an OSS Semgrep-fork that runs as a CLI **inside the existing in-cluster runners** — no server, no database, no Elasticsearch, no license, no plugin. Rules-as-code; the runner posts PR feedback (SARIF/check-run) via the GitHub App token `lightbridge-repo-auth` already mints. No chart/namespace/CNPG/Keycloak/values entry — a CI concern, not a workload. Trade-off: rule-matching SAST only (no trend dashboards/coverage/quality-gate-over-time); rule curation is ours. Open: rule home, gating threshold, output path, repo scope. Rejected alts: SonarQube (0065), upstream Semgrep (relicensed), CodeQL. | Proposed | 2026-06-27 | relates to [0065](./0065-sonarqube-community-build-with-branch-plugin.md); builds on [0047](./0047-github-oidc-repo-binding-for-ci.md)/[0049](./0049-source-claim-operator-cli-not-self-serve.md) |
| [0067](./0067-jwt-token-consumption-dashboard.md) | **JWT-token-level consumption dashboard (email from the JWT claim only)**: per individual JWT (`oidc_jti`) — cost/tokens/requests + last usages — with email taken from the JWT `email` label ONLY (not the Keycloak directory; thin tokens show their `missing:*`/`unstamped:*` sentinel honestly). **Loki-backed** because `oidc_jti` is an access-log **body** field, deliberately never a Mimir label (ADR-0064: per-token cardinality). Decision: a generated **`jwt-tokens` dashboard** (stats, top-JWTs-by-cost/tokens bargauge legend `<email>·<jti>`, cost-per-JWT timeseries, a last-usages logs panel). ⚠️ LogQL contract: extract `oidc_jti` in the SAME `\| json` the outer `sum by` groups on (else it collapses to `-`); dotted usage keys via bracket form; `\| __error__=""` on every unwrap; range (not instant) queries; 6h default + `topk(20)`. Cardinality verified low live (~handful of jti/hour). No new infra/grants. | Accepted | 2026-06-27 | builds on [0046](./0046-per-user-attribution-otlp-envelope-repair.md), [0011](./0011-oidc-downstream-headers.md), [0058](./0058-precompute-gateway-usage-metrics-to-mimir.md), [0064](./0064-keycloak-sessions-and-grants-visibility.md), [0008](./0008-python-dashboard-generation.md) |
| [0068](./0068-synthetic-identities-for-known-service-callers.md) | **Structured synthetic identities for known non-human callers (extends ADR-0052)**: ADR-0052 made absent identity claims loud, but lumped genuine *gaps* (a Keycloak human with no email) with *known* service callers whose identity was already on the request. Decision: at the **Authorino** layer (not Grafana), synthesize a structured identity for known non-human callers — **email = `<resource>@<service>`** (GitHub CI → `<owner/repo>@gh-runners` from the `repository` claim; LCI → `<owner/repo>@lightbridge-code-intelligence` from `x-code-intelligence-repo`; LibreChat keeps its real forwarded email) and **jti = `<kind>:<id>`** (gh → `runid:<run_id>` `has()`-guarded; LCI → `runid:<task-id>`; LibreChat → `librechat:<x-librechat-user>`, was `missing:librechat:jti`). Services become first-class **named** identities on the cost/JWT boards; the human/service split stays an Authorino-set `billing_plan`/`azp` label (so synthetic service emails are intentionally NOT excluded by `(missing\|unstamped):.*`). Scope = **email + jti only** (budgets/`x-account-id` untouched). Genuine gaps keep `missing:keycloak:*`/`missing:service:*`. CEL-only, no dashboard change; lives in `ai-helm-values` security-policies.yaml. ⚠️ `run_id` claim guarded + needs live CI confirmation. | Accepted | 2026-06-27 | extends [0052](./0052-source-qualified-missing-claim-sentinels.md); builds on [0011](./0011-oidc-downstream-headers.md), [0046](./0046-per-user-attribution-otlp-envelope-repair.md), [0047](./0047-github-oidc-repo-binding-for-ci.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0067](./0067-jwt-token-consumption-dashboard.md) |
| [0069](./0069-adopt-envoy-ai-gateway-v1.md) | **Adopt Envoy AI Gateway v1.0 + migrate AIEG manifests to `v1beta1`**: v1.0 is the stability milestone (no breaking changes; `v1beta1` declared stable, byte-identical to v0.7's). Bump `aieg-crd`/`aieg` `v0.7.0→v1.0.0` and `eg` `v1.8.0→v1.8.1` (new floor); migrate our six AIEG kinds (`MCPRoute`, `AIGatewayRoute`, `AIServiceBackend`, `BackendSecurityPolicy`, `GatewayConfig`/`List`) off the now-deprecated `v1alpha1` → `v1beta1` (apiVersion line only; superset schema; already the v0.7 storage version → no ordering hazard). The upstream `gateway.envoyproxy.io` group is **separate** and untouched. Wire v1.0's MCP authz surface into `charts/mcp` as **opt-in/default-off** capability — `toolFilter`→`toolSelector`, `forwardHeaders`, CEL `authorization.rules`; activate `forwardHeaders: [x-oidc-user-id]` on the self-hosted brave/terraform. External-MCP Caddy/openresty proxies **stay** (#2218 open, #2219 unfixed in v1.0). | Accepted | 2026-06-27 | builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0038](./0038-mcp-oauth-protected-resource-metadata.md), [0034](./0034-restore-streaming-timeouts-and-extproc-headroom.md), [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md); retains [0040](./0040-external-mcps-via-caddy-normalizing-proxy.md), [0041](./0041-firecrawl-protocol-version-rewrite-nginx-engine.md) |
| [0070](./0070-ratelimit-quota-observability.md) | **Rate-limit quota observability — read the limiter's live counters from Redis**: the Envoy AI Gateway's per-account budget counters (ADR-0021/0035) live ONLY in redis-ha — Mimir/Loki hold historical cost, not the limiter's current-window state. New **`AI Gateway — rate-limit quota`** dashboard with TWO read paths over the same keys: (1) a new `observability` child **`prometheus-redis-exporter`** SCANs the monthly-budget keys (`db0=*rule-2-match-0*` free, `*rule-7-match-0*` pro — stable budget rule indices, verified live) → `gateway_ratelimit_spend_micro_usd`; ServiceMonitor `metricRelabelings` parse `account_id/model/plan/plane/window` out of the 200-char key (`window` KEPT as a label so the lingering previous bucket doesn't collide → duplicate-sample error); Mimir leaderboard ranks spend per account/model. (2) A **`redis-datasource`** GrafanaDatasource (`tlsAuth:true` ENABLES the TLS dial — no client cert; verifies the internal CA already mounted for the LLM plugin) for a zero-scrape-lag `tmscan` census. **Raw consumption only** (limits are static config, plan not on the key). Connects to the HAProxy master-router, TLS-only. Chart logic in ai-helm; datasource + exporter values + deps in ai-helm-values (values-repo-first). | Accepted | 2026-06-27 | builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0035](./0035-per-person-monthly-budget-and-free-50.md), [0058](./0058-precompute-gateway-usage-metrics-to-mimir.md), [0020](./0020-observability-app-of-apps-orchestrator.md), [0056](./0056-workload-values-in-ai-helm-values.md), [0008](./0008-python-dashboard-generation.md) |
| [0071](./0071-local-npx-mcp-servers-and-role-subagents.md) | **Local `npx` MCP servers + their role subagents** in the opencode well-known: add three NPM-ONLY servers as `type: local` (opencode spawns each via `npx` over stdio, client-side — no gateway route, no OAuth anchor) — `@modelcontextprotocol/server-memory` (custom `MEMORY_FILE_PATH={env:HOME}/.local/share/opencode/memory.json`), `@modelcontextprotocol/server-sequential-thinking`, `@mobilenext/mobile-mcp@latest`. Each follows the ADR-0044/0048 deny-then-re-allow discipline: added to the global deny-baseline (off the lean primary) and re-allowed only on a NEW dedicated subagent — `@memory` (graph r/w, no edit/bash), `@planner` (read-only reasoning + context7), `@mobile` (device driver; pins multimodal `adorsys-frontend` since it reads screenshots, per the ADR-0048 no-risk vision rule). NOT version-pinned (separate processes behind a tool boundary → float like the remotes). Python-backed requests (mem0, MCP-Mathematics → uvx; pdfmux → npm wrapper needing `pip install pdfmux` + an LLM key) DEFERRED to a later batch. | Accepted | 2026-06-29 | builds on [0042](./0042-opencode-wellknown-mcp-catalog.md), [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md), [0048](./0048-global-browser-plugin-and-per-agent-tool-injection.md), [0038](./0038-mcp-oauth-protected-resource-metadata.md) |
| [0072](./0072-no-key-local-mcp-batch.md) | **No-key local MCP batch** in the opencode well-known: add six `type: local` (`npx`) servers selected for ZERO setup (no API key/token/signup → work for every user day one) — `git` (`@cyanheads/git-mcp-server`, local git, fills the deprecated-GitHub-server gap), `drawio` (`@drawio/mcp`, official, diagrams incl. Mermaid, no binary), `shadcn` (`@jpisnice/shadcn-ui-mcp-server`, keyless), `reddit` (anonymous read), `youtube` (`@sinco-lab/mcp-youtube-transcript`), `rss` (`rss-mcp`). Four new subagents (deny-baseline off the primary, allow only there): `@vcs` (git), `@ui` (shadcn), `@diagram` (drawio), `@content` (reddit+youtube+rss+webfetch — one marketing-research agent). All non-visual → no model pin. Token-gated servers (Notion/Figma/search/Sentry/…) deferred to opt-in batches; GitHub + rich Atlassian/Linear are remote-only → deferred as gateway `/mcp` routes (not npx-able). Driven by a verified-against-npm research pass (devs/marketing/PO). | Accepted | 2026-06-29 | builds on [0071](./0071-local-npx-mcp-servers-and-role-subagents.md), [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md), [0048](./0048-global-browser-plugin-and-per-agent-tool-injection.md), [0042](./0042-opencode-wellknown-mcp-catalog.md); GitHub/Atlassian routes will use [0038](./0038-mcp-oauth-protected-resource-metadata.md)/[0040](./0040-external-mcps-via-caddy-normalizing-proxy.md) |
| [0073](./0073-issue-tracker-mcps-atlassian-local-github-gateway.md) | **Issue-tracker MCPs** in the opencode well-known, a HYBRID by runtime: **Jira/Confluence as per-user local `npx`** (`@aashari/mcp-server-atlassian-jira` + `-confluence`, both reading the user's own `ATLASSIAN_SITE_NAME`/`ATLASSIAN_USER_EMAIL`/`ATLASSIAN_API_TOKEN` via `{env:...}` passthrough — set once, shared by both), denied on the primary and re-allowed only on a new `@atlassian` subagent **(phase 1, implemented)**; and **GitHub via a centralized GitHub-App gateway `/mcp/github` route** because GitHub has no viable npx server (official npm one deprecated → Go binary) — self-host `github/github-mcp-server` in-cluster + AIEG MCPRoute + Keycloak JWT, App credential out-of-band in ssegning-aws, values-repo-first **(phase 2, planned)**. Shared GitHub App identity (fine for read; shared write attribution). | Accepted | 2026-06-29 | builds on [0071](./0071-local-npx-mcp-servers-and-role-subagents.md), [0072](./0072-no-key-local-mcp-batch.md), [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md), [0048](./0048-global-browser-plugin-and-per-agent-tool-injection.md); phase 2 uses [0038](./0038-mcp-oauth-protected-resource-metadata.md), [0040](./0040-external-mcps-via-caddy-normalizing-proxy.md) |
| [0074](./0074-opencode-opt-in-mcps-and-multi-primary-fleet.md) | **Opencode well-known: MCPs opt-in + a multi-primary fleet**: two knobs flip in `charts/librechat-opencode-wellknown`. (1) **Every one of the 16 catalogued MCP servers ships `enabled: false`** (opt-in) — a fresh `opencode auth login` no longer auto-connects 4 remotes + ~11 cold-`npx` locals (no surprise OAuth prompts, lean startup, honest tool surface); the deny-baseline + per-server subagent are kept INTACT so enabling one is a one-line flip. (2) **`frontend` is demoted from the sole default to one of a FLEET of `mode: primary` agents**, new neutral default **`assistant`** (named NOT `general` — that's a built-in subagent, like `build`/`plan` are built-in primaries): assistant / frontend / backend / devops / marketing (non-dev research; named NOT `content` = the subagent) / docs / ux / architect / enemy (hard adversarial red-team) / tester / security. Each keeps the ADR-0048 lean discipline (no MCP re-allow, no `model` pin, delegates to the 16 subagents); adding a primary is cheap because tools live behind subagents. | Accepted | 2026-06-30 | amends [0048](./0048-global-browser-plugin-and-per-agent-tool-injection.md) (single primary → fleet), [0071](./0071-local-npx-mcp-servers-and-role-subagents.md)/[0072](./0072-no-key-local-mcp-batch.md)/[0073](./0073-issue-tracker-mcps-atlassian-local-github-gateway.md) (servers flip to `enabled:false`); builds on [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md), [0042](./0042-opencode-wellknown-mcp-catalog.md) |
| [0075](./0075-glm-5.2-price-drop-consolidate-glm-catalog.md) | **GLM-5.2 price drop — consolidate GLM-5/GLM-5.1 onto GLM-5.2**: DeepInfra's GLM-5.2 price dropped from the $1.40/$0.25/$4.40 evaluated in ADR-0050 to **$0.93/$0.18/$3.00**, now undercutting GLM-5.1 ($1.05/$0.205/$3.50) on every axis with a 1,048,576-token context (vs GLM-5.1's 202,752). Repoints `reviewer`, `adorsys-planner`, `adorsys-planner-pro`, `adorsys-coder-pro` from GLM-5/GLM-5.1 to GLM-5.2; corrects `adorsys-reviewer-pro`'s input price to the confirmed rate; consolidates the standalone `glm-5`/`glm-5p1` catalog ids into one `glm-5p2`. Known side effect: `adorsys-planner`/`-pro` now share an identical backing (flagged, not blocking). | Accepted | 2026-07-02 | amends the GLM-5.2 non-adoption call in [0050](./0050-kimi-k2.7-code-supersedes-k2.6-as-frontend-pro-backing.md) (Kimi K2.7 `adorsys-frontend-pro` decision unaffected); refines [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md) |
| [0076](./0076-remove-grafana-llm-assistant.md) | **Remove the Grafana LLM assistant (no viable OSS use-case)**: ADR-0062's `grafana-llm-app`→gateway backend was made to work end-to-end (a `base`→gemma-4 completion returned 200, after fixing `provider: custom` not `openai`, and Cilium egress to the gateway's backend port `:10443` not the Service `:443`, plus a one-off Authorino re-index). But `grafana-llm-app` is only an LLM *backend* — self-hosted OSS Grafana has **no** AI chat UI (the Assistant is Cloud-only), "Explain Flame Graph" needs Pyroscope data we don't collect, and the sole OSS chat front-end (`vikshana-graft-app`) is an unsigned third-party plugin declined on security grounds. With no consumer, remove the whole feature from `ai-helm-values` (plugin, LLM gateway key + provisioning ConfigMap, the `internal-key-grafana` apiKey, the `:10443` egress). **Retain** the `grafana-internal-ca` cert + `/etc/ssl/certs` mount — repurposed by [0070](./0070-ratelimit-quota-observability.md) for the Grafana `Redis` datasource's TLS trust. Future in-Grafana AI, if wanted: `mcp-grafana` (no unsigned plugin) or a signed chat app. | Accepted | 2026-06-29 | supersedes [0062](./0062-grafana-llm-assistant-via-internal-gateway.md); the retained cert relates to [0070](./0070-ratelimit-quota-observability.md) |
| [0077](./0077-my-usage-dashboard.md) | **Personal usage dashboard with built-in user variable and folder-level RBAC**: end users see only their own AI gateway token/cost usage via a new `my-usage` Grafana dashboard that filters all LogQL queries on `user_id="${__user.login}"` — a server-controlled built-in variable (not a URL variable) that maps to the Keycloak `sub` UUID via `login_attribute_path = sub` in the `generic_oauth` config (ai-helm-values repo). Folder-level RBAC via `GrafanaFolder.spec.permissions`: `ai-gateway` folder restricted to Admin+Editor (Viewer denied — protects admin dashboards), `my-usage` folder open to Viewer=1. Viewers cannot access Explore in Grafana OSS, closing the IDOR vector. Rejected: URL-variable isolation (IDOR — `user_id` controllable via URL param), auth proxy (same OSS Explore gap), per-user dashboards (do not scale). | Accepted | 2026-07-03 | builds on [0023](./0023-grafana-stateless-no-pvc.md), [0008](./0008-python-dashboard-generation.md); config dependency in ai-helm-values (`login_attribute_path = sub`) |
| [0077](./0077-phoenix-style-chat-dashboards.md) | **Phoenix-style chat dashboards on the AI Gateway's existing OpenInference traces**: discovered (live, undocumented) that the AI Gateway ext-proc has been emitting full-content OpenInference spans to Tempo all along — `OTEL_EXPORTER_OTLP_ENDPOINT` was already set for the ext-proc's own instrumentation, and OpenInference defaults to capturing full request/response text (`llm.input_messages.*`/`llm.output_messages.*`), confirmed against live Tempo tags — the same true in Phoenix's era (ADR-0002), since both backends received identical spans. Adds two generated dashboards: `chat-overview` (no input, embeddings excluded, Loki/Mimir aggregates + a native Tempo `type="traces"` panel scoped to `span.kind=LLM`) and `chats-by-user` (a `$user` picker sourced from the Keycloak read-only Postgres datasource, ADR-0063; Loki aggregates only, deliberately no trace/content panel). A `controller.spanRequestHeaderAttributes` mapping to tag spans with `user.id`/`user.email` was built and opened as a PR in the private `ai-helm-values` repo, then **closed unmerged** — live verification (reading raw trace content directly) confirmed content access never depended on it, so the mapping would only have bought TraceQL-level per-user filtering at a real privacy cost, not worth it. Single-chat drill-down dropped: no LibreChat conversation id is forwarded anywhere in the pipeline yet. | Accepted | 2026-07-07 | builds on [0002](./0002-replace-phoenix-with-tempo.md), [0008](./0008-python-dashboard-generation.md), [0046](./0046-per-user-attribution-otlp-envelope-repair.md), [0063](./0063-grafana-readonly-keycloak-datasource.md); attribution decision superseded by [0078](./0078-per-user-span-attribution-for-chat-content.md) → [0079](./0079-per-user-span-attribution-not-viable.md) |
| [0078](./0078-per-user-span-attribution-for-chat-content.md) | **Adopt per-user span attribution for chat content** (reverses ADR-0077's rejection): set `controller.spanRequestHeaderAttributes: "x-oidc-user-id:user.id,x-oidc-email:user.email"` so the ext-proc tags spans with the Keycloak identity Authorino stamps, unblocking a per-user Tempo trace panel filtered by `span.user.email`. **Deployed end-to-end but produced nothing — superseded by [0079](./0079-per-user-span-attribution-not-viable.md).** | Superseded by [0079](./0079-per-user-span-attribution-not-viable.md) | 2026-07-07 | superseded the attribution decision in [0077](./0077-phoenix-style-chat-dashboards.md); built on [0011](./0011-oidc-downstream-headers.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0063](./0063-grafana-readonly-keycloak-datasource.md) |
| [0079](./0079-per-user-span-attribution-not-viable.md) | **Per-user span attribution is not viable — the AIEG ext-proc runs before Authorino**: ADR-0078's `spanRequestHeaderAttributes` mapping was deployed end-to-end and produced NOTHING. Pulling the live Envoy filter chain proved why: the AI Gateway ext-proc (`ext_proc/aigateway`) is HTTP filter **#1**, six positions **before** Authorino's `ext_authz` — so it captures request headers for the span before `x-oidc-*` are injected (the access log sees them only because `%REQ%` reads the final header state). AIEG places its ext-proc first by design (raw-body/model parsing). Decision: revert the (no-op) `spanRequestHeaderAttributes`, drop the permanently-"No data" trace panel, keep `chats-by-user` as the working per-request Loki metadata log; read content in the global `chat-overview`. The `filterOrder` reorder (ext_proc after ext_authz) was declined — a global reorder on the prod gateway affecting LLM + MCP routes, with a suffixed filter name and routing/transform risk, outweighs the feature. No privacy change (nothing was ever attributed). | Accepted | 2026-07-08 | supersedes [0078](./0078-per-user-span-attribution-for-chat-content.md); relates to [0038](./0038-mcp-oauth-protected-resource-metadata.md) (MCP routes on the same listener) |
| [0080](./0080-mermaid-mcp-universal-and-global-diagram-directive.md) | **Mermaid MCP available to every agent + a global "explain via diagrams" directive**: diagram-as-explanation is a cross-cutting output style, not a specialist capability — the LibreChat/opencode UIs render a ` ```mermaid ` fenced block client-side with no tool call. Add the `mermaid` server (`npx -y mcp-mermaid`, `type: local`, stdio) `enabled: true` — the one server on by default after ADR-0074 flipped the other 16 to opt-in — and deliberately OMIT `mermaid_*` from the deny-baseline so opencode injects its (single, tiny) tool into every primary AND every subagent (exercising ADR-0074's own "graduate a universally-useful server back to enabled" follow-up; the heavy servers stay opt-in + subagent-scoped, so ADR-0048's lean-context contract holds). Because opencode has no top-level inline prompt and `instructions` takes file paths a well-known merge can't ship, the "explain via graphs" directive is appended verbatim to all 27 agent prompts (guarded script, each anchor asserted to match exactly once). Image *rendering* needs local chromium (playwright via `mermaid-isomorphic`) — same trade-off as @browser/@mobile; the client-side fenced-block path needs nothing. | Accepted | 2026-07-08 | amends [0074](./0074-opencode-opt-in-mcps-and-multi-primary-fleet.md) (graduates `mermaid` to `enabled: true` + universal); builds on [0044](./0044-opencode-role-subagents-and-permission-scoped-tools.md), [0048](./0048-global-browser-plugin-and-per-agent-tool-injection.md), [0042](./0042-opencode-wellknown-mcp-catalog.md) |
| [0081](./0081-a2a-agent2agent-server-via-gateway.md) | **A2A (Agent2Agent) agent platform — dynamic registry, Restate runtime, EAIG-fronted** (PROPOSAL, Rev 2 — rewritten in place while Proposed; Rev 1's static chart-per-agent server survives only in Alternatives): a **multi-tenant agent-hosting service** — tenants register agents at runtime and get a custom URL `/a2a/<agent-id>`, unlike models. Five layers: a Rust axum **protocol plane** (A2A crate spike: a2a-rs/ra2a/a2a-rust; cards + JSON-RPC + SSE) behind ONE wildcard `HTTPRoute` on `api-https` (no AIEG A2A route type exists; dynamic card paths exempted from Authorino via a pattern-based allow-all SecurityPolicy — the ADR-0038 trick, regex form); a Postgres **registry** (agents = DB rows, not Git; ownership by `x-account-id` — EAIG owns inbound auth, the backend never verifies JWTs); a **managed runtime** = `rig-core` loops inside `restate-sdk` handlers on self-hosted **Restate** (durable-execution journal ↔ resumable turns, awakeables ↔ `input-required`/push, virtual objects ↔ `contextId`; model calls via the internal plane → governed + cost-attributed; redis pub/sub relays token streams to SSE); a **client plane + A2A→MCP bridge** (opencode/LibreChat are MCP-native; outbound token acquisition is our own code); **logs** = structured stdout → Alloy node-tail → Loki (egress-immune; agent/account as stream labels via one relabel rule in ai-helm-values, `taskId` in body — cardinality). Rejected: k8s Jobs (input-required = interactive pause/resume), Coder Tasks/Agents (deprecating v2.37, coding-specific, removed ADR-0027), hand-rolled state machine (Restate wins on Rust macros + single binary). Open: Restate placement (home-os vs ai-helm), task-level cost model, signed cards, crate maturity. ~9–13+ wk. | Proposed | 2026-07-09 | builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0038](./0038-mcp-oauth-protected-resource-metadata.md), [0069](./0069-adopt-envoy-ai-gateway-v1.md), [0011](./0011-oidc-downstream-headers.md); relates to [0034](./0034-restore-streaming-timeouts-and-extproc-headroom.md), [0017](./0017-home-remote-destination-invariant.md), [0056](./0056-workload-values-in-ai-helm-values.md), [0027](./0027-mcps-orchestrator-split-and-coder-removal.md) |
| [0082](./0082-release-please-changelog-and-minor-major-floor.md) | **release-please for changelogs + the `MAJOR.MINOR` floor (hybrid with ADR-0055)**: adopt `googleapis/release-please-action@v5` (manifest mode, `release-type: helm`, one package per tracked chart) to maintain per-chart `CHANGELOG.md`s + cut GitHub Releases and to propose the deliberate `MAJOR.MINOR` floor bumps — **without** disturbing ADR-0055's derive-at-publish model. Works because `publish-charts-oci.yml` reads only `MAJOR.MINOR` and always recomputes `PATCH` from commit-count: release-please owns the floor + changelog, the publish job still owns (and commits nothing back for) the deployed `PATCH`. `bump-minor-pre-major: true` + `bump-patch-for-minor-pre-major: false` → `feat`→minor, breaking→major (pre-1.0: minor), `fix`→patch (which publish discards, so `fix`-only PRs are changelog-only). One aggregated release PR; default `GITHUB_TOKEN` (so the PR trips no CI/governance); a human merge → publish deploys the floor bump (no loop). `bjw-common`/`bjw-template` **excluded** (dependents exact-pin `4.6.2` → a bump would break `helm dependency build`); `common` (`*`-consumed) + `apps` stay tracked. `bootstrap-sha` pinned so the first PR isn't a giant retroactive changelog. | Accepted | 2026-07-10 | amends [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md) |
| [0084](./0084-ratelimit-plan-order-is-append-only.md) | **Rate-limit plan order is an append-only list, not a sorted map**: the gateway-wide monthly-budget `BackendTrafficPolicy` renders one rule per plan, and the rule's POSITION becomes the `rule/N` in the Lyft ratelimit redis key while the plan name is masked (`Exact` match ⇒ constant) — so the index is the only carrier of plan identity. `plans` was a **map**, and Helm sorts map keys, so adding `enterprise` on 2026-07-16 ([#663](https://github.com/ADORSYS-GIS/ai-helm/issues/663)) did not append but **inserted at index 0**, pushing `free` 0→1 and `pro` 1→2 **mid-window**: every counter was orphaned and the whole fleet silently got a fresh monthly budget (confirmed live — one account holding `rule/0`+`rule/1`+`rule/2` in window `1783296000`). Convert `plans` to an **ordered list** of `{id, monthlyBudgetUsd}` pinned to the live order (`enterprise`, `free`, `pro`) and treat it as **append-only**; add a `fail` guard on missing `id` and emit the index into the rendered comment. **Render-neutral** (parsed manifest byte-identical ⇒ re-keys nothing, mergeable any day). Rejected: a second `Distinct` selector putting the plan name in the key (CRD allows it, but EG v1.8.2 translation of two selectors on one header is unverified AND it re-keys everything ⇒ would need a window boundary); numeric-prefixed map keys (the key IS the `x-billing-plan` value). Follow-up: `charts/ai-models` `rateLimitBudgeting.plans` has the same flaw, currently masked by `sharedBudget.enabled`. | Accepted | 2026-07-20 | builds on [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md), [0035](./0035-per-person-monthly-budget-and-free-50.md), [0070](./0070-ratelimit-quota-observability.md) |
| [0085](./0085-mlops-platform-lakefs-argo-workflows-mlflow.md) | Self-hosted MLOps platform (LakeFS + Argo Workflows + MLflow): reuse `lightbridge-main-db` CNPG + the existing Hetzner Object Storage bucket/creds for all three; auth is per-app not uniform — Argo Workflows native `server.sso`, MLflow's bundled `mlflow-oidc-auth` plugin (native, discovered mid-implementation), LakeFS a dedicated oauth2-proxy (OSS has no functional OIDC — accepted gap: LakeFS's own internal user/credential system stays separate) | Accepted | 2026-07-22 | builds on [0018](./0018-umbrella-apps-and-env-overlays.md), [0083](./0083-coder-re-introduction.md), [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md), [0056](./0056-workload-values-in-ai-helm-values.md) |
| [0086](./0086-librechat-agent-fleet-and-gitops-seed.md) | **LibreChat agent fleet (subagents) + GitOps seed via `/api/agents`**: curate specialist *leaf* subagents (coder/reviewer/researcher/frontend/iac/PM/platform-concierge) + *orchestrator* primaries (`Builder`/`Analyst`/`Platform Copilot`) that delegate via the `subagents` capability. Agents are DB objects (no `librechat.yaml` schema — verified v0.8.7) requiring a real `author` User, so seed them from a run-once idempotent Job on the LibreChat image: ensure a platform user, mint a `JWT_SECRET`-signed token, **GET-by-name → PATCH/POST** (server generates `agent.id` ⇒ POST isn't idempotent), **two-phase** (leaves first, then orchestrators referencing resolved leaf ids). Also expose `terraform`/`context7`/`refero` gateway MCPs (reuse `self-service-mcp-api` client `*` redirect ⇒ no keycloak change); omit `brave`/`firecrawl` (redundant with built-in webSearch). ⚠️ Seed path is stateful — needs a live smoke test, can't be verified from the chart repo. | Proposed | 2026-07-24 | builds on [0038](./0038-mcp-oauth-protected-resource-metadata.md), [0074](./0074-opencode-opt-in-mcps-and-multi-primary-fleet.md), [0075](./0075-glm-5.2-price-drop-consolidate-glm-catalog.md), [0027](./0027-mcps-orchestrator-split-and-coder-removal.md) |
| [0087](./0087-librechat-app-config-to-values-repo.md) | **Move librechat-app workload config to `ai-helm-values`**: finish the ADR-0056 split for the `librechart` leaves. Move the ~900-line LibreChat `config:` block (models/MCPs/model-specs/token-config/skill-sync/interface) to `environments/prod/values/librechat-app.yaml`; teach the `librechart` ApplicationSet per-child `valuesFromRepo` (a `$values` source + `helm.valueFiles` + `ignoreMissingValueFiles`, like the observability/lightbridge App-of-Apps got); strip the inline `config:` from the chart. `librechatSecrets`/`env`/ExternalSecrets stay chart-owned. ⚠️ Values-repo-FIRST + must be built AFTER the in-flight config PRs (#724 skill-sync, #725 icon-URLs) merge, or it reverts them / falls LibreChat back to broken defaults. | Proposed | 2026-07-24 | builds on [0056](./0056-workload-values-in-ai-helm-values.md), [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md), [0014](./0014-split-librechart-and-opencode-wellknown.md) |
| [0088](./0088-agent-seed-job-system-user-and-visibility.md) | **Agent-seed Job — SSO system user as author + public-permissions visibility**: resolves ADR-0086's two open pins so the fleet is buildable. Author = the `platform@ai.camer.digital` SSO user (log in once → its Mongo `_id`; Job mints a `JWT_SECRET` LibreChat token `{id}`). Visibility = `PUT /api/agents/<id>/permissions {public:true, publicAccessRoleId:"agent_viewer"}` (same PUBLIC-principal grant skill-sync uses for `SKILL_VIEWER`). Run-once ArgoCD `Sync`-hook Job on the LibreChat image: idempotent GET-by-name→PATCH/POST, **two-phase** (leaves → orchestrators with `subagents.agent_ids`), fails closed if the user doc is absent. Starter fleet = `deep-reviewer` → `security-reviewer` + `test-coverage-reviewer` (DB Agents, the `code-review` skill), distinct from the flat `reviewer` modelSpec. Fleet specs in ai-helm-values (`agentSeed.agents[]`); Job+script in `charts/librechat-app`. ⚠️ Stateful — first run supervised (can't verify from the chart repo). | Proposed | 2026-07-24 | builds on [0086](./0086-librechat-agent-fleet-and-gitops-seed.md), [0056](./0056-workload-values-in-ai-helm-values.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md) |
| [0089](./0089-homepage-central-hub-oauth2-proxy.md) | **Homepage central hub, gated by a dedicated oauth2-proxy**: [Homepage](https://gethomepage.dev) as a central dashboard for the growing app list, App-of-Apps orchestrator (`homepage-secrets`/`homepage-auth`/`homepage-app`, own `homepage` namespace). Homepage ships NO authentication of its own, so it gets a dedicated reverse-proxy-mode oauth2-proxy — the SAME shape ADR-0085's `lakefs-auth` used before removal, but here it's the sole gate, not a redundant second one (LakeFS already had native login; Homepage doesn't). Content: hybrid curated bookmarks + k8s auto-discovery via `gethomepage.dev/*` annotations (RBAC per upstream's own documented shape). Uptime: reuses the existing Mimir/Grafana stack via Homepage's native `prometheus` widget — no new polling pipeline. | Accepted | 2026-07-25 | builds on [0018](./0018-umbrella-apps-and-env-overlays.md), [0019](./0019-coder-app-of-apps-orchestrator.md), [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md), [0056](./0056-workload-values-in-ai-helm-values.md); contrasts with [0085](./0085-mlops-platform-lakefs-argo-workflows-mlflow.md) |
| [0090](./0090-lakefs-sso-via-lakefs-proxy-shim.md) | **Keycloak SSO for LakeFS via the `lakefs-proxy` session shim**: LakeFS OSS 1.83 rejects `auth.oidc.*` outright (Enterprise-only) and, with `rbac: none`, is single-user — verified live. A bare oauth2-proxy is therefore a double login (why [0085](./0085-mlops-platform-lakefs-argo-workflows-mlflow.md)'s `lakefs-auth` was removed in #755). Fix: a third hop — `lakefs-auth` (oauth2-proxy, reverse-proxy) → **`lakefs-proxy`** (new leaf chart, first-party Rust shim) → `lakefs`. The shim logs in as the bootstrap admin and RELAYS LakeFS's own `Set-Cookie` (a Go securecookie, not a JWT — unmintable). ⚠️ Accepted limitation: authentication is per-Keycloak-user, but in-LakeFS authorization + audit are SHARED on the one `admin` identity (all commits show as `admin`). Runbook + troubleshooting: [lakefs-sso.md](../playbooks/lakefs-sso.md). | Accepted | 2026-07-25 | amends [0085](./0085-mlops-platform-lakefs-argo-workflows-mlflow.md); builds on [0019](./0019-coder-app-of-apps-orchestrator.md), [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md), [0056](./0056-workload-values-in-ai-helm-values.md); same proxy shape as [0089](./0089-homepage-central-hub-oauth2-proxy.md) |
| [0091](./0091-mlops-programmatic-access-and-bearer-audience.md) | **Gate MLflow bearer tokens on `aud`, and open a least-privileged programmatic path to Argo Workflows**: three live findings against [0085](./0085-mlops-platform-lakefs-argo-workflows-mlflow.md)'s "RBAC minimal for v1". (1) MLflow accepted ANY realm token on its API — `mlflow-oidc-auth` only checks `aud` when `OIDC_AUDIENCE` is set, and the group gate runs on the browser-callback path only ⇒ `oidcAuth.audience: mlflow`. (2) Workflow pods ran as `mlops/default` and every step failed on `workflowtaskresults` (chart default `workflow.serviceAccount.create: false` against `rbac.create: true` ⇒ a RoleBinding naming an SA that never existed) ⇒ create the `argo-workflow` SA **plus** `controller.workflowDefaults.spec.serviceAccountName` (the SA alone is not enough — pods default to `default`), and enable `agentPermissions`/`artifactGC` (silent-hang and finalizer-wedge failure modes). (3) `authModes: [sso]` matches ONLY the `Bearer v2:` JWE argo-server mints in its own callback, so no script could reach the API ⇒ add **`client`** (the least-privileged mode: the server acts AS the caller under ordinary namespaced RBAC) + a dedicated `argo-workflows-ci` SA with **no** long-lived token Secret (`kubectl create token --duration`). LakeFS-from-workflows is documented, not templated: mount the existing `lakefs-proxy-admin` Secret — effectively LakeFS root, shared with the [0090](./0090-lakefs-sso-via-lakefs-proxy-shim.md) shim. | Accepted | 2026-07-25 | amends [0085](./0085-mlops-platform-lakefs-argo-workflows-mlflow.md); builds on [0090](./0090-lakefs-sso-via-lakefs-proxy-shim.md), [0056](./0056-workload-values-in-ai-helm-values.md) |
| [0092](./0092-longhorn-for-hetzner-gpu-nodes.md) | **Longhorn scoped to the Hetzner Robot GPU nodes only, via foreign-provider node isolation**: the hand-joined GPU nodes (`hetzner-k8s-gpu-1/2`) carry a foreign `providerID` and `is-root-server` label specifically so the hcloud CCM and `hcloud-csi-node` leave them alone (Robot boxes aren't in the hcloud Cloud API) — the consequence is they have NO CSI driver at all. Deploys Longhorn (`aii-longhorn`, `home-remote`, `longhorn-system`) pinned to those 2 nodes ONLY via `global.nodeSelector`/`tolerations` (static components) **and** the separate Longhorn-internal `defaultSettings.taintToleration`/`systemManagedComponentsNodeSelector` (dynamic instance-manager/engine-image pods) + `createDefaultDiskLabeledNodes`, so it can never touch the working hcloud-csi setup elsewhere. `numberOfReplicas: 2` (2-node topology today, no Raft/quorum concern), V1 engine only, `reclaimPolicy: Retain` (model caches are expensive to re-fetch from S3), and deliberately NOT the cluster-default StorageClass (that role stays with hcloud-csi's `hcloud-volumes` — workloads on the GPU nodes opt in explicitly via `storageClassName: longhorn`). `longhorn-system` added to `global.namespacePodSecurity` as `privileged` (hostPath/iSCSI, same mechanism as Alloy). ⚠️ Distinct from the unrelated pre-existing Longhorn on `admin@homeos` (backs `model-serving-qwen3-4b`/`zimage-turbo`) — same product name, two separate deployments. | Accepted | 2026-07-25 | builds on [0017](./0017-home-remote-destination-invariant.md), [0018](./0018-umbrella-apps-and-env-overlays.md), [0022](./0022-self-hosted-gpu-model-federated-into-gateway.md), [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md), [0056](./0056-workload-values-in-ai-helm-values.md) |
| [0093](./0093-longhorn-ui-oauth2-proxy-role-restricted.md) | **Gate the Longhorn UI with a dedicated, role-restricted oauth2-proxy**: the Longhorn UI has no native auth and grants full volume/node management (delete included) — a materially higher-stakes surface than `homepage-auth`'s read-only dashboard. Public Ingress (`longhorn.ai.camer.digital`, `cert-home-cert-http`) fronted by a dedicated oauth2-proxy (own Keycloak client `longhorn-proxy`), but UNLIKE `homepage-auth`: access is restricted to a Keycloak client role (`admin`), mapped via a **default** client scope into a **multivalued** `longhorn_roles` claim (mirrors `mlflow_roles`/`argo_workflows_roles` — multivalued is load-bearing per ADR-0085's lesson), enforced with `--oidc-groups-claim=longhorn_roles --allowed-group=admin`. Structurally a 2-child orchestrator (`longhorn-secrets` + `longhorn-auth`, sync-waves 0/1) — deliberately NOT a 3-child orchestrator like `homepage`, since the Longhorn workload is the separate, already-live flat `aii-longhorn` Application (ADR-0092) and restructuring it would force ArgoCD to delete/recreate the whole running stack for no benefit. Both children colocate in the existing `longhorn-system` namespace, which has no NetworkPolicy baseline today (verified live) — no CiliumNetworkPolicy needed, and retrofitting one is explicitly out of scope. | Accepted | 2026-07-25 | builds on [0092](./0092-longhorn-for-hetzner-gpu-nodes.md), [0089](./0089-homepage-central-hub-oauth2-proxy.md), [0085](./0085-mlops-platform-lakefs-argo-workflows-mlflow.md), [0018](./0018-umbrella-apps-and-env-overlays.md), [0055](./0055-oci-charts-and-image-updater-writeback-to-values-repo.md), [0056](./0056-workload-values-in-ai-helm-values.md) |
| [0094](./0094-generic-model-serving-orchestrator.md) | **Replace the per-model serving charts with one orchestrator plus a generic leaf**: self-hosted serving had grown to EIGHT copy-pasted `charts/model-serving-<model>` charts (~300 values lines each) differing only in a model id, an HF repo and a few engine flags — plus a per-model `charts/apps` entry and, because there was one GPU, an `enabled: false` swap dance across four files. Adopts the ADR-0012 orchestrator-plus-leaves shape: `charts/model-serving` emits ONE ApplicationSet with a child per catalog entry, `charts/model-server` is the generic leaf (ADR-0030 hybrid bjw shape retained). Crucially the ENGINE PROFILES (`llamacpp`, `vllm`+opt-in LMCache) live in the ORCHESTRATOR's `_helpers.tpl`, not the leaf — a Helm parent cannot compute SUBCHART values at render time, which is exactly why every old chart hardcoded its seed repo/glob under a "⚠️ keep in sync" comment (a chart could serve one set of weights while downloading another). GPU placement becomes a `nvidia.com/gpu: 1` resource request, so N+1 models queue as `Pending` instead of needing a human swap. Adding/replacing a model = ONE ~15-line entry in `charts/model-serving/values.yaml`. `tools/check-model-catalogs.sh` fails CI if a cluster-local gateway backend has no server behind it. Legacy `model-serving-*` charts are RETAINED (zimage-turbo is live on `admin@homeos`), marked legacy, and must not be copied. | Accepted | 2026-07-26 | builds on [0012](./0012-split-ai-models-applicationset.md), [0030](./0030-merge-model-and-proxy-into-one-statefulset-bjw.md), [0092](./0092-longhorn-for-hetzner-gpu-nodes.md), [0095](./0095-cluster-local-model-federation.md) |
| [0095](./0095-cluster-local-model-federation.md) | **Federate self-hosted models over the cluster network, not a public edge**: ADR-0022's public edge (Traefik Ingress + `cert-cloudflare` DNS-01 cert + DNS record + static API key + a Caddy auth-proxy sidecar for engines that ignore the key + `homeCluster: true`) existed solely because the GPU was on a DIFFERENT cluster from the gateway. The Hetzner GPU nodes are on `home-remote` itself (ADR-0092), so all of it is deleted: the gateway `Backend` points at `<model>.inference.svc.cluster.local:8080` over plain HTTP, with no `tlsHostname` (⇒ no BackendTLSPolicy) and no credential (⇒ no BackendSecurityPolicy — needed a guard in `charts/ai-models-backends`, which previously emitted a typeless policy for credential-less backends). Access control becomes a `CiliumNetworkPolicy`: ingress only from `envoy-gateway-system` + `observability`, plus the `host`/`remote-node`/`health` entities carrying KUBELET PROBE traffic (omit those and the pod never goes Ready). Stronger than the key it replaces — the model has no route off the cluster to bypass the gateway with. These are ordinary `home-remote` workloads, so the ADR-0017 `homeCluster` exception is now scoped to the legacy `admin@homeos` generation and should retire with it. All auth/budgets/rate-limits/metering stay at the gateway (ADR-0021), unchanged. | Accepted | 2026-07-26 | amends [0022](./0022-self-hosted-gpu-model-federated-into-gateway.md), narrows [0017](./0017-home-remote-destination-invariant.md); builds on [0092](./0092-longhorn-for-hetzner-gpu-nodes.md), [0094](./0094-generic-model-serving-orchestrator.md), [0021](./0021-burst-budget-billing-and-dual-plane-authconfigs.md) |
| [0096](./0096-gex44-fleet-cost-recovery-pricing.md) | **Cost-recovery pricing for the rented GEX44 GPU fleet**: ADR-0028's principle stands but every input to its TCO formula is wrong for rented hardware — no capex to amortize, power included in the rent, and one model owns the whole node. Replaces it for rented nodes with `€/hour = monthly rent ÷ 730` (€184/mo ⇒ **€0.252/h, 5× the A2000 basis**), then maps to per-token using the **measured** 15.05 tok/s rather than an assumed rate: saturated floor €4.65/1M output, × the ~3.45 utilization uplift implied by the existing Qwen3-4B entry ⇒ **$17.00 / $2.55 / $0.50** (out/in/cached). Deliberately high and deliberately not fudged — a slow model on a rented card costs ~an order of magnitude more per token than the SaaS entries, which is precisely the make-vs-buy signal ADR-0028 exists to surface. ⚠️ €184/mo is Hetzner's LIST price, not a confirmed invoice; and the idle second node is a fleet-utilization question, not a per-token one. | Accepted | 2026-07-26 | extends [0028](./0028-owned-hardware-model-pricing.md); prices the fleet of [0094](./0094-generic-model-serving-orchestrator.md)/[0095](./0095-cluster-local-model-federation.md) |
| [0097](./0097-engine-agnostic-serving-hardening.md) | **An engine-agnostic hardening baseline for model servers**: ADR-0095 removed the static API key because it only defended a public endpoint that no longer exists — sound, but it did not account for the ENGINE's own defaults. llama-server warns at start-up that CORS is `*` and no API key is set, and it serves a full chat **Web UI + static file server on the API port, enabled by default**. Defines the hardening as fleet POLICY (`charts/model-serving` `defaults.security`) mapped to per-engine flags in `_helpers.tpl`: engine checks a Bearer itself (`--api-key-file` / `VLLM_API_KEY`), no browser UI (`--no-webui`; vLLM satisfies this by shipping none), no wildcard CORS (`--cors-origins`). Defaults ON with a PER-MODEL opt-out rather than fleet opt-in — the key and CORS pin cost nothing, and `disableWebUI` is the only knob with debugging value (and even that is moot in the normal path, since the NetworkPolicy already makes the UI unreachable). Model and gateway backend must move in lockstep on the key or every request 401s. ⚠️ vLLM's `--allowed-origins` is specified but NOT enabled pending verification on a live pod. Amends [0095](./0095-cluster-local-model-federation.md); the NetworkPolicy remains the primary control. | Accepted | 2026-07-26 | amends [0095](./0095-cluster-local-model-federation.md); builds on [0094](./0094-generic-model-serving-orchestrator.md) |
| [0098](./0098-deployment-recreate-instead-of-statefulset.md) | **Model servers run as a Deployment with `Recreate`, not a StatefulSet**: ADR-0030 considered a Deployment "equivalent for replicas: 1" — equivalent for the single-GPU guarantee, NOT in failure. A StatefulSet defaults to `podManagementPolicy: OrderedReady`, under which the controller will not replace a not-Ready pod, so a **crash-looping pod blocks its own fix**: the corrected chart merges and syncs, `.spec.template` holds the new args, and the pod runs the old ones indefinitely (`currentRevision != updateRevision`) until someone runs `kubectl delete pod`. Documented k8s behaviour ("Forced rollback"), and it bit us for real on the vLLM tier. We use NO StatefulSet-only feature — `volumeClaimTemplates` is empty (the weights PVC is our own RWX claim via `existingClaim`), no ordinals, no stable network name — so the gate bought nothing. `strategy: Recreate` is load-bearing, not cosmetic: it fully terminates the old pod first, so two pods never contend for the single `nvidia.com/gpu: 1` (under RollingUpdate the new pod would wait for a card the old pod still holds — a deadlock). Cost: pod names lose their ordinal (`deploy/<model>-main` now), and a one-time restart of both models. | Accepted | 2026-07-26 | amends [0030](./0030-merge-model-and-proxy-into-one-statefulset-bjw.md); returns to the shape [0029](./0029-self-hosted-model-plain-deployment.md) used |
| [0099](./0099-grafana-branding-within-oss-limits.md) | **Brand Grafana within OSS limits; do not white-label**: the assumption that Grafana can be configured for custom branding is WRONG in two independent ways. (1) `[white_labeling]` — logo, login background, footer, app title, favicon — is **Grafana Enterprise only**; on the OSS image we run (`grafana/grafana:12.3.1`, verified live) those keys are **silently ignored**. (2) The org NAME, the org's home-dashboard preference and org theme all live in Grafana's DATABASE, and this Grafana is stateless (`persistence.enabled: false`, ADR-0023) — so setting them via UI/API appears to work and then reverts on the next pod roll, which is worse than being unable to set them. What IS durable is `grafana.ini` plus grafana-operator CRs, so: an adorsys landing dashboard set as `[users] home_page` (NOT the org preference, and not `default_home_dashboard_path` which needs a file mounted in the pod), `default_theme`, and disabling the Grafana Labs news feed / update check / usage reporting. Brand colour + logo live inside the landing dashboard, which also carries orientation links. ⚠️ Creates a cross-repo contract: `home_page` in ai-helm-values points at uid `adorsys-platform-home` provisioned from ai-helm — the dashboard must exist first or everyone lands on a 404. Explicitly REJECTED: overwriting Grafana's static assets (unsupported, breaks silently on image bumps, binary blob under GitOps, failure mode is a broken image in prod), proxy CSS injection, and forking the image (owning CVE rebuilds for a logo). Enterprise is deferred as a budget decision, not an engineering one. | Accepted | 2026-07-27 | constrained by [0023](./0023-grafana-stateless-no-pvc.md); builds on [0008](./0008-python-dashboard-generation.md), [0045](./0045-scrape-first-dashboard-sourcing.md) |
| [0100](./0100-image-generation-on-the-gpu-fleet.md) | **Image generation joins the GPU fleet as a third engine profile; OpenMythos-27B retired to free the card**: Z-Image-Turbo (Tongyi-MAI, 6B distilled DiT, OpenAI `/v1/images/generations`) moves off the home RTX A2000 — where PR #704 had left it behind a public `*--poc.ssegning.com` edge with a static Bearer — onto the Hetzner GPU fleet as an ordinary ~20-line catalog entry. Adds `defaults.engines.zimage` to `charts/model-serving`, proving ADR-0094's contract ("an engine = a profile + a `ci/` fixture") holds for an engine that is neither a language model nor an upstream image: no context window, no tokens, no metrics, and a FIRST-PARTY image (`images/z-image-turbo-server/`, Rust + Candle). Three engine facts become profile DATA rather than `if engine == …` branches: `apiKey.mode: file|env` (llama.cpp reads a file, vLLM/zimage an env var), `metrics: false` (no Prometheus endpoint — a ServiceMonitor would scrape a 404 forever), `devShm` (vLLM only). Fixes four defects inherited from PR #704, each of which alone made the deployment non-functional: a LAZY model load deadlocking against a readiness-gated `/health` (not-Ready ⇒ no endpoint ⇒ no request ⇒ never loads), a 20 GiB PVC for ~33 GB of weights (the published repo is FP32, NOT the advertised "FP8"), a Dockerfile that cannot build (`cargo build` with no `RUN`, `COPY tests/` for a directory that does not exist, `-fuse-ld=lld` without lld), and CUDA 12.8 + sm_86 kernels against the fleet's driver 550 / CUDA 12.4 / sm_89 Ada cards. ⚠️ **BUILD-FIRST**: nothing in CI builds this image (same as `lakefs-proxy`) — push the tag before merging the entry or the pod sits in ImagePullBackOff. ⚠️ Two accepted gaps: the ADR-0097 CORS pin is unenforceable (the server hardcodes `allow_any_origin()`, declared `corsConfigurable: false`), and the price ($0.005/image) is a PLACEHOLDER with no ADR-0096 cost basis until images/hour is measured. `up{namespace="inference"}` is now blind to half the fleet, so a new engine-independent `ms-model-unavailable` alert reads kube-state-metrics instead. Consequence: every legacy `charts/model-serving-*` chart is now disabled, removing ADR-0094's stated reason for retaining them. | Accepted | 2026-07-27 | builds on [0094](./0094-generic-model-serving-orchestrator.md), [0095](./0095-cluster-local-model-federation.md), [0097](./0097-engine-agnostic-serving-hardening.md), [0098](./0098-deployment-recreate-instead-of-statefulset.md); supersedes the deployment shape of [0022](./0022-self-hosted-gpu-model-federated-into-gateway.md) for this model; pricing basis [0096](./0096-gex44-fleet-cost-recovery-pricing.md) |
| [0101](./0101-load-gate-before-federation-no-exceptions.md) | **The load gate has no migration exception — a model is not federated until it is measured on the hardware it runs on**: amends [0100](./0100-image-generation-on-the-gpu-fleet.md)'s federation choice ONLY (the `zimage` engine profile, the first-party image, the move off the home GPU and OpenMythos's retirement all stand). ADR-0100 kept Z-Image-Turbo federated through its move on the argument that a MIGRATION is not a new offering, so pulling it would be a visible regression. That was wrong: the model had never been built, seeded or loaded on this hardware, so what federation preserved was a route to a backend with nothing behind it — the regression had already happened, and the exception only made it silent. Two size estimates then failed on the first sync while users could still select the model (seed memory OOM-killed 4× in 6 min; the weights volume hit 100% with 44 GB in a staging dir and the model dirs empty), and a THIRD estimate derived exactly the same way — ~12.4 GB GPU-resident of 20475 MiB — is the one that decides whether the model runs at all. So: `z-image-turbo-local` `enabled: false` until a report exists in `inference-ops docs/benchmarks/`, with the price re-derived from measured images/hour at the same time rather than carrying the `$0.005/image` placeholder forward. The rule, now written down: **"it worked on the old hardware" is not evidence about the new hardware** — if anything it is the case that most deserves a gate, because it carries a false sense of having been proven. ⚠️ `check-model-catalogs.sh` keys on BACKENDS, not model entries, so it still counts this model as federated and is not a witness to this decision. | Accepted | 2026-07-27 | amends [0100](./0100-image-generation-on-the-gpu-fleet.md); reinstates the discipline in [0094](./0094-generic-model-serving-orchestrator.md); pricing [0096](./0096-gex44-fleet-cost-recovery-pricing.md) |
| [0102](./0102-localai-instead-of-a-first-party-image-server.md) | **Serve image generation with LocalAI; delete the first-party server**: supersedes [0100](./0100-image-generation-on-the-gpu-fleet.md)'s ENGINE choice (everything else there stands). ADR-0100's alternatives analysis weighed `diffusers`-as-a-library, ComfyUI and SwarmUI and concluded nothing off-the-shelf spoke `/v1/images/generations` — it never considered the category that exists to solve exactly that: OpenAI-compatible multi-backend servers. Verified against primary sources: **LocalAI** ships the endpoint and carries FOUR Z-Image gallery entries (`z-image-turbo-diffusers`, `Z-Image-Turbo` ggml, `z-image-diffusers`, `vllm-omni-z-image-turbo`); **Xinference** has it as a builtin. ⚠️ Pin `quay.io/go-skynet/local-ai:v4.7.1-gpu-nvidia-cuda-12` — `master`/`latest` are built against **CUDA 13** against our driver 550 / CUDA 12.4 (same trap as `server-cuda13` / `-cu129`). Two new profile keys, as DATA not engine branches: `seedJob: false` (LocalAI downloads its own model AND its inference backend, so no seed Job, no HF token, no pinned revision) and `writableModelStore: true` (so the volume must be writable and persist `backends/`). `metrics` returns to **true** — LocalAI serves `/metrics` behind its admin middleware, which the ServiceMonitor's existing Bearer covers — closing the observability gap. Cost of the original call, in one day: ~1000 lines of Rust to maintain, a hand-built image with a build-first tax, an unenforceable CORS policy, three follow-up PRs (#792 seed OOM, #793 staging disk, #794 un-federation) and a user-facing outage. ⚠️ GIVES UP the pinned-SHA guarantee (a gallery reference can move) and, with no seed Job, bjw names the Deployment `<model>` not `<model>-main`, so ADR-0098's standard runbook commands miss for image models. | Accepted | 2026-07-28 | supersedes the engine choice in [0100](./0100-image-generation-on-the-gpu-fleet.md); builds on [0101](./0101-load-gate-before-federation-no-exceptions.md), [0097](./0097-engine-agnostic-serving-hardening.md) |
| [0103](./0103-own-the-localai-model-config.md) | **Own LocalAI's model config instead of naming a gallery entry**: amends [0102](./0102-localai-instead-of-a-first-party-image-server.md) — same engine, we just stop delegating its configuration. Pointing at a gallery entry is one string and hands over three decisions, all of which cost once measured: the gallery ships `step: 25` for a model **distilled for 8 steps** (~3× the work for no quality gain) and `offload_params_to_cpu: true`, which produced **815 MiB of VRAM used out of 20475** — a 20 GiB card idle while the PCIe bus did the work. A catalog entry may now set `serving.modelConfig`, rendered into a ConfigMap by `charts/model-server` and applied via `MODELS_CONFIG_FILE`; `MODELS` is unset because there is no gallery entry left to install. ✅ **Recovers the pinned-bytes guarantee** ADR-0102 recorded as the main cost of adopting LocalAI — `download_files` takes a `sha256` per file, so the bytes are pinned by us, in git, in a reviewable diff. ⚠️ `MODELS_CONFIG_FILE`, **not** `PRELOAD_MODELS_CONFIG`: the latter is a list of GALLERY SOURCES each needing a `url`, and feeding it raw model configs dies at boot with `unsupported protocol scheme ""` — an error describing a network problem that is really a missing field. ⚠️ The ConfigMap mounts OUTSIDE the weights volume (MODELS_PATH is writable and LocalAI owns it). ⚠️ Cost: we now own tuning we did not write, and will not get upstream's improvements for free. Does NOT fix the unpinned/unsigned runtime BACKEND, which is a separate problem. | Accepted | 2026-07-28 | amends [0102](./0102-localai-instead-of-a-first-party-image-server.md); pricing [0096](./0096-gex44-fleet-cost-recovery-pricing.md) needs superseding separately |
| [0104](./0104-gpu-cost-basis-correction.md) | **The GPU cost basis was wrong by ~18%, fleet-wide**: supersedes [0096](./0096-gex44-fleet-cost-recovery-pricing.md) (basis only — the METHOD is unchanged). Every self-hosted price derives from one number, `€184/mo ÷ 730h`, taken from a published list price and flagged as unconfirmed at the time. The GEX44 is **~$234/mo ≈ €217**, so €/hour goes 0.2521 → **0.2968** and every `*-local` price was ~18% low — `qwen3-8b-fast` included ($0.88/$0.18/$5.85 → $1.04/$0.21/$6.89), not just the image model that prompted the check. Three commitments: **cite an invoice, not a list price**; **re-derive from measured throughput rather than scaling by 1.18** (scaling would bake stale measurements into a corrected basis); and the **3.45× duty-cycle uplift stays FLAGGED** — it is inherited from the old A2000 and has never been measured on this fleet, so one input in the formula is now known-wrong-and-fixed while the other remains unverified. ⚠️ Consequence: user-visible price rises on every self-hosted model, landing on internal consumers who did not cause the error. | Accepted | 2026-07-28 | supersedes [0096](./0096-gex44-fleet-cost-recovery-pricing.md); method [0028](./0028-owned-hardware-model-pricing.md) |
| [0105](./0105-pin-and-verify-the-localai-backend.md) | **Pin and verify the LocalAI backend; define the model ourselves**: closes both gaps ADR-0102 recorded and could not solve. Read from the binary's own `--help`, LocalAI resolved its inference backend from `…/index.yaml@master` at image tag `latest` with **no signature check** — so the component that answers HTTP was pinned while the component that EXECUTES THE MODEL floated. Now: gallery `@v4.7.1`, image tag `v4.7.1` (the server's own release, so the two move together or not at all), and a keyless-cosign policy (issuer + identity regex + `not_before`, the revocation lever — keyless certs are ephemeral, there is no CA-side revocation). Simultaneously drops `galleryModel` and defines the model via `download_files` with a **sha256 per file**, which RESTORES the pinned-bytes guarantee ADR-0102 called the main cost of adopting LocalAI, and collapses `/v1/models` from two image models to one. The chart now fails the render if `modelConfig` is set without `galleryModel` and without `backends` — dropping the gallery model removes the backend install it performed as a side effect, and LocalAI reports that absence as a cooldown cascade naming every backend EXCEPT the missing one. ⚠️ `requireBackendIntegrity` deliberately left UNSET (it turns a missing policy anywhere into a hard boot failure — wants one fresh-volume test), and the cosign identity is unproven here because the backend is already on the volume: if the regex is wrong it fails on a fresh volume, at the worst moment. | Accepted | 2026-07-28 | closes gaps in [0102](./0102-localai-instead-of-a-first-party-image-server.md); completes [0103](./0103-own-the-localai-model-config.md) |
| [0106](./0106-restore-the-localai-image-tier.md) | **Restore the LocalAI image tier, and delete the chart that displaced it**: [PR #790](https://github.com/ADORSYS-GIS/ai-helm/pull/790) merged a branch cut before [0100](./0100-image-generation-on-the-gpu-fleet.md) and never rebased. It merged CLEANLY and silently reverted 29 commits and ADRs [0100](./0100-image-generation-on-the-gpu-fleet.md)–[0105](./0105-pin-and-verify-the-localai-backend.md), reinstating `charts/model-serving-zimage-turbo` — which references `ghcr.io/adorsys-gis/z-image-turbo-server:v0.1.0`, an image that **does not exist** (ADR-0102 deleted its source the day after it shipped, and no CI ever built it). The pod sat in `ImagePullBackOff` from merge until disabled, while three follow-up PRs (#799 tolerations, #802 seed OOM, #814 dead federated backend) each fixed a symptom of the revert. Takes issue [#803](https://github.com/ADORSYS-GIS/ai-helm/issues/803)'s **path (b)**: restore the LocalAI configuration that was already tuned and measured on this hardware (**32.4 s / 1024×1024, 7985 MiB of 20475**, image generated and looked at) rather than finish a first-party server ADR-0102 decided against on evidence that still holds. ⚠️ The legacy chart is **DELETED, not disabled** — it was disabled once already and a stale branch turned it back on, so *a disabled chart is a resurrection surface*; it had also stopped being a rollback surface, since you cannot roll back to an image that does not exist. ⚠️ Weights PVC drops 100Gi → 40Gi: the 100 was never a size decision but a floor forced by an existing claim (Longhorn never shrinks), and that claim was pruned — verified live. ⚠️ **The next boot is a fresh-volume install**, so ADR-0105's two untested paths (the `download_files` fetch and the cosign identity regex) are now on the critical path; re-confirm one generation after the first sync. **Process lesson:** a long-lived branch that merges cleanly can still revert everything merged after it was cut, and every gate stays green — reverting good config is not a syntax error. Rebase first, and read the diff against `main`, not the branch's own history. | Accepted | 2026-07-29 | restores [0102](./0102-localai-instead-of-a-first-party-image-server.md)/[0103](./0103-own-the-localai-model-config.md)/[0105](./0105-pin-and-verify-the-localai-backend.md); gate [0101](./0101-load-gate-before-federation-no-exceptions.md); retires the chart [0100](./0100-image-generation-on-the-gpu-fleet.md) disabled |
| [0107](./0107-rename-model-serving-to-inference.md) | **`model-serving`/`model-server` → `inference`/`inference-server`**: the GPU-serving charts sat in a cluster of near-homonyms — `model-serving` (the ADR-0094 orchestrator), `model-server` (the generic leaf, two characters apart), and seven **legacy** `model-serving-<model>` per-model charts whose suffix reads as a variant rather than a different generation. `inference` matches the workload namespace and the `inference-ops` docs repo, and `inference` vs `inference-server` differ by a whole word. Renamed with them: the Helm template namespaces, the leaf's OCI path in the ApplicationSet, the `charts/apps` Application + release name, `.trivyignore.yaml`, `tools/check-model-catalogs.sh`, and every mutable doc. ⚠️ **Child Applications are recreated**: their names are `{{ .Release.Name }}-<model>`, so ArgoCD prunes `model-serving-qwen3-vl-4b-thinking` (cascading to its PVC) and creates `inference-qwen3-vl-4b-thinking` — **~9 GB of weights re-download and that model is down for the window**, accepted as a one-time cost. ⚠️ `oci://ghcr.io/adorsys-gis/charts/inference` does not exist until `publish-charts-oci` runs on merge, so the new app is briefly unresolvable. **Deliberately NOT renamed:** the seven legacy charts (the old name is now the legacy marker), accepted ADRs 0010–0106 (immutable — this ADR is the mapping), `docs/patterns/self-hosted-model-serving.md` + `docs/architecture/09-model-serving.md` (linked from immutable ADRs), and the Grafana folder + `team: model-serving` alert label (a routing taxonomy, its own blast radius). | Accepted | 2026-07-29 | renames the charts from [0094](./0094-generic-model-serving-orchestrator.md); landed with [0106](./0106-restore-the-localai-image-tier.md) |
| [0108](./0108-localai-backend-verification-is-unsatisfiable.md) | **LocalAI's backend signature verification is unsatisfiable — drop it, and correct 0105's pinning claim**: supersedes the *verification* decision of [0105](./0105-pin-and-verify-the-localai-backend.md) ONLY (its gallery pin, own-model-config and `download_files` sha256 weight pinning all stand). 0105 honestly recorded its cosign policy as **unproven** because the volume was already populated; [0106](./0106-restore-the-localai-image-tier.md) created the fresh volume and it fired within minutes — `CrashLoopBackOff`, no endpoints, while the model was federated and advertised in `/v1/models` (the [0101](./0101-load-gate-before-federation-no-exceptions.md) failure mode again). ⚠️ **The prediction was right and the diagnosis in it was wrong, which is the point**: it is NOT the identity regex, which is never reached. Checked against the quay referrers API rather than inferred — the backend image **is** signed, in the **legacy cosign** format (config `application/vnd.dev.cosign.artifact.sig.v1+json`, layer `…cosign.simplesigning.v1+json`), while LocalAI v4.7.1's verifier reads only the **new Sigstore bundle** format (`no Sigstore bundle referrer … signed with --new-bundle-format?`). Upstream signs one way and its own verifier reads the other, so no issuer/regex/`not_before` could have worked; v4.7.1 is the newest release, so there is no version to bump into, and nothing upstream reports it. ⚠️ **A SECOND 0105 claim also fails**: the backend **image tag** was never pinned — the gallery index at `@v4.7.1` hardcodes `uri: "…local-ai-backends:latest-gpu-nvidia-cuda-12-stablediffusion-ggml"` and LocalAI uses it verbatim, so `--backend-images-release-tag` never applies to a gallery-sourced backend. Today `latest-…` and `v4.7.1-…` resolve to the same digest (`sha256:58bf31fe…`) so we run the measured bytes, but nothing holds that tomorrow. Net: the backend binary is **unverified and unpinned**; the MODEL's weights stay sha256-pinned and the gallery INDEX stays pinned. `requireBackendIntegrity` goes from *deferred* to *load-bearing* — it would hard-fail every boot now. Durable fix (follow-up): verify upstream's legacy signature in CI, mirror by digest into our registry, re-sign with `cosign --new-bundle-format` under our GHA identity, host our own gallery index — plus an upstream issue asking mudler to sign in the format their own verifier reads. | Accepted | 2026-07-29 | supersedes the verification decision of [0105](./0105-pin-and-verify-the-localai-backend.md); exposed by [0106](./0106-restore-the-localai-image-tier.md); gate [0101](./0101-load-gate-before-federation-no-exceptions.md) |
| [0109](./0109-ready-means-served-warmup-startup-probe.md) | **Ready means *served*: gate the image engine's startup probe on a real generation.** [0100](./0100-image-generation-on-the-gpu-fleet.md) listed lazy model loading as one of four fatal defects and treated it as closed by the LocalAI swap plus `LOAD_TO_MEMORY`. **Measured, `Ready` does not mean loaded**: at Ready with no request sent, `/readyz` 200s and the Service has an endpoint while nvidia-smi reports **5 MiB** used. ⚠️ **This is NOT an upstream bug** — read in the v4.7.1 source, `LOAD_TO_MEMORY` → `PreloadModelByName` → `ml.Load()` genuinely spawns the backend and opens the model (the `stablediffusion-ggml` process IS live at Ready, confirmed with `ps`); what defers is **stable-diffusion.cpp's upload of tensors to the GPU** at first inference, one layer BELOW LocalAI's abstraction — so no LocalAI setting reaches it and there is no config fix to prefer. Ruled out and each checked: the idle watchdog (defaults off, unset), the `SingleBackend` guard (unset, so preload runs), preload no-opping (it calls `ml.Load`), and "weights are only resident while generating" (disproved — 7985 MiB flat across 190 s idle). Cost **32.03 s cold vs 29.23 s warm** at 1024×1024, paid by a user; the deeper problem is an endpoint that lies about readiness ([0101](./0101-load-gate-before-federation-no-exceptions.md) with a green dashboard on top). Fix: an engine may declare a `warmup` block (profile DATA per [0094](./0094-generic-model-serving-orchestrator.md)) and its startup probe performs a real inference request; `llamacpp`/`vllm` omit it and keep httpGet. ⚠️ **We own this** — ~15 lines of our Helm, not a LocalAI feature, accepted knowingly after confirming no upstream knob exists. ⚠️ 256×256 is deliberate (resolution does not change WHICH weights load: ~3.8 s, VRAM 5 → 7505 MiB, held). ⚠️ The warmup must present the API key or every probe 401s and the pod never goes Ready. ⚠️ `"n": 1` is QUOTED — unquoted `n` is a YAML 1.1 boolean, so it rendered `{"false":1,…}`. ⚠️ Evidence lesson: `nvidia-smi` inside a container prints `No running processes found` even when a process is using the GPU (PID namespaces) — trust its memory figure, use `ps` for processes. | Accepted | 2026-07-29 | closes the lazy-load defect [0100](./0100-image-generation-on-the-gpu-fleet.md) believed fixed; discipline of [0101](./0101-load-gate-before-federation-no-exceptions.md); found closing out [0106](./0106-restore-the-localai-image-tier.md)/[0108](./0108-localai-backend-verification-is-unsatisfiable.md) |

## Status legend

- **Proposed** — written, not yet implemented; can still change.
- **Rejected** — written, considered, and decided against; never implemented (kept for the record).
- **Accepted** — implemented or being implemented; treated as the current truth.
- **Deprecated** — no longer the recommended path, but still in effect (e.g. partly migrated away).
- **Superseded** — replaced by a later ADR; carry a `Supersedes` link and a `Superseded by` link in both records.

## Writing a new ADR

1. Copy [`template.md`](./template.md) to `NNNN-short-imperative-title.md` (next free number, zero-padded to 4).
2. Fill in Context, Decision, Consequences. Keep it short — 1–2 pages.
3. List Alternatives considered with WHY they were rejected (this is often the most valuable section).
4. Open the PR with `Status: Proposed`. Move to `Accepted` when the implementation lands (same PR or a follow-up).
5. Update the index above.

## Scope: what deserves an ADR

Write one when you make a choice that:
- Has consequences a future reader would want to understand without git-archeology.
- Locks in a contract (CRD version, naming convention, label key).
- Trades off something non-obvious (cost vs. ergonomics, lock-in vs. simplicity, type-safety vs. cardinality).

Don't write one for:
- "Use Helm" — read the repo, that's obvious.
- "Bump grafana to 11.2" — that's a release note.
- "Add a NetworkPolicy to mcpo" — that's a bug fix or a chart README change.
